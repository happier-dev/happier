// GENERATED by scripts/i18n-generate-entries.mjs from home.tsx — do not edit.
import { mount } from './_mount';
import { registerOverlay } from '../i18n/siteData';
import ns_comparison from '../i18n/generated/slices/pt/comparison.json';
import ns_faq from '../i18n/generated/slices/pt/faq.json';
import ns_features from '../i18n/generated/slices/pt/features.json';
import ns_navigation from '../i18n/generated/slices/pt/navigation.json';
import ns_pageProse from '../i18n/generated/slices/pt/pageProse.json';


// Registered here, not discovered by a glob: this entry is the only place
// that knows the page is pt, which keeps the other nine locales out of
// its download. The imports above are the translation NAMESPACES this route
// was measured to read — scripts/i18n-slice-overlays.mjs renders the page to
// find them and proves the set reproduces its HTML byte for byte. Rollup
// shares each namespace chunk between the routes that import it.
registerOverlay('pt', Object.assign({}, ns_comparison, ns_faq, ns_features, ns_navigation, ns_pageProse));

/**
 * `/` — the client entry for the homepage, and the template for all of them.
 *
 * An entry names ONE page and hands it to mount(). It must not import
 * src/routes.tsx: that file reaches every page component on the site, which is
 * the single import that would put all 21 pages back in one download.
 *
 * The locale is a literal because the file is per (route, locale): the day `/`
 * gains a translation, src/routes.tsx grows `locales`, routeManifest() emits a
 * second entry for it, and the build asks for src/entries/zh-Hans--home.tsx.
 */
mount('pt');
