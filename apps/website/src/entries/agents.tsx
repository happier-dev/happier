import { AgentsIndex } from '../pages/AgentsIndex';
import { mount } from './_mount';

/** `/agents` — see src/entries/home.tsx for what an entry may and may not import. */
mount('en', <AgentsIndex />);
