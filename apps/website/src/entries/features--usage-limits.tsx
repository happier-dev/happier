import { UsageLimitsPage } from '../pages/UsageLimitsPage';
import { mount } from './_mount';

/** `/features/usage-limits` — see src/entries/home.tsx for what an entry may and may not import. */
mount('en', <UsageLimitsPage />);
