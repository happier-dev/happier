/** GENERATED data-only registration identities for bundled first-party Agents. */
import type { PluginContributionIdentityV1 } from '@happier-dev/protocol/plugins/contribution-identity';

export type BundledFirstPartyAgentRegistrationBinding = Readonly<{
  identity: PluginContributionIdentityV1;
  implementationOwnerId: string;
  registrationFamily: string;
}>;

export const BUNDLED_FIRST_PARTY_AGENT_REGISTRATION_BINDINGS: readonly BundledFirstPartyAgentRegistrationBinding[] = Object.freeze([
  Object.freeze({
    identity: Object.freeze({
      pluginId: "happier.agent.antigravity",
      localId: "antigravity",
    }),
    implementationOwnerId: "antigravity",
    registrationFamily: "agents",
  }),
  Object.freeze({
    identity: Object.freeze({
      pluginId: "happier.agent.auggie",
      localId: "auggie",
    }),
    implementationOwnerId: "auggie",
    registrationFamily: "agents",
  }),
  Object.freeze({
    identity: Object.freeze({
      pluginId: "happier.agent.claude",
      localId: "claude",
    }),
    implementationOwnerId: "claude",
    registrationFamily: "agents",
  }),
  Object.freeze({
    identity: Object.freeze({
      pluginId: "happier.agent.codex",
      localId: "codex",
    }),
    implementationOwnerId: "codex",
    registrationFamily: "agents",
  }),
  Object.freeze({
    identity: Object.freeze({
      pluginId: "happier.agent.copilot",
      localId: "copilot",
    }),
    implementationOwnerId: "copilot",
    registrationFamily: "agents",
  }),
  Object.freeze({
    identity: Object.freeze({
      pluginId: "happier.agent.cursor",
      localId: "cursor",
    }),
    implementationOwnerId: "cursor",
    registrationFamily: "agents",
  }),
  Object.freeze({
    identity: Object.freeze({
      pluginId: "happier.agent.gemini",
      localId: "gemini",
    }),
    implementationOwnerId: "gemini",
    registrationFamily: "agents",
  }),
  Object.freeze({
    identity: Object.freeze({
      pluginId: "happier.agent.grok",
      localId: "grok",
    }),
    implementationOwnerId: "grok",
    registrationFamily: "agents",
  }),
  Object.freeze({
    identity: Object.freeze({
      pluginId: "happier.agent.kilo",
      localId: "kilo",
    }),
    implementationOwnerId: "kilo",
    registrationFamily: "agents",
  }),
  Object.freeze({
    identity: Object.freeze({
      pluginId: "happier.agent.kimi",
      localId: "kimi",
    }),
    implementationOwnerId: "kimi",
    registrationFamily: "agents",
  }),
  Object.freeze({
    identity: Object.freeze({
      pluginId: "happier.agent.kiro",
      localId: "kiro",
    }),
    implementationOwnerId: "kiro",
    registrationFamily: "agents",
  }),
  Object.freeze({
    identity: Object.freeze({
      pluginId: "happier.agent.ohmypi",
      localId: "ohmypi",
    }),
    implementationOwnerId: "ohMyPi",
    registrationFamily: "agents",
  }),
  Object.freeze({
    identity: Object.freeze({
      pluginId: "happier.agent.opencode",
      localId: "opencode",
    }),
    implementationOwnerId: "opencode",
    registrationFamily: "agents",
  }),
  Object.freeze({
    identity: Object.freeze({
      pluginId: "happier.agent.pi",
      localId: "pi",
    }),
    implementationOwnerId: "pi",
    registrationFamily: "agents",
  }),
  Object.freeze({
    identity: Object.freeze({
      pluginId: "happier.agent.qwen",
      localId: "qwen",
    }),
    implementationOwnerId: "qwen",
    registrationFamily: "agents",
  }),
  Object.freeze({
    identity: Object.freeze({
      pluginId: "happier.review.coderabbit",
      localId: "coderabbit",
    }),
    implementationOwnerId: "coderabbit",
    registrationFamily: "agents",
  }),
  Object.freeze({
    identity: Object.freeze({
      pluginId: "happier.review.deepsec",
      localId: "deepsec",
    }),
    implementationOwnerId: "deepsec",
    registrationFamily: "agents",
  }),
]);
