---
title: Host actions
description: Generated reference for every host Action available through the Plugin Actions service.
---

{/* Generated from the canonical ActionSpec registry. Do not hand-edit. */}

Use this reference when calling host Actions from a Plugin invocation context. It is generated from the canonical ActionSpec registry, so each listed id is available through `context.services.actions.execute(...)` and no hand-maintained Action list can drift from the host.

```ts
const result = await context.services.actions.execute('action.spec.get', {
  id: 'session.status.get',
}, {
  signal: context.signal,
});
```

The Action service validates the exact input and result contract for each id. Use the input guidance below when it is present; the TypeScript API remains the final typed contract.

## `account.apiTokens.create`

**Create API token** — Create a named API token for the current Account. The token secret is shown once.

- Safety: `danger`; side effect: `danger`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `api`.

### Input guidance

- `label` (text, required): Label
- `expiresAt` (text): Expiry

## `account.apiTokens.list`

**List API tokens** — List non-secret API-token summaries for the current Account.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `account.apiTokens.revoke`

**Revoke API token** — Revoke one API token for the current Account.

- Safety: `danger`; side effect: `danger`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `api`.

### Input guidance

- `tokenId` (text, required): Token id

## `account.apiTokens.revokeAll`

**Revoke all API tokens** — Revoke every API token for the current Account. Signed sessions are unaffected.

- Safety: `danger`; side effect: `danger`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `account.plugins.data.erase`

**Erase Account plugin data** — Erase the current Account’s retained data for one plugin without uninstalling local plugin code.

- Safety: `danger`; side effect: `danger`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `api`.

### Input guidance

- `pluginId` (text, required): Plugin id

## `account.sessions.signOutEverywhere`

**Sign out everywhere** — Invalidate all signed sessions for the current Account. API tokens remain active.

- Safety: `danger`; side effect: `danger`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `action.invoke`

**Invoke contributed action** — Invoke one currently available contributed Action through the canonical host dispatcher. If the result is \`denied\`, the person declined the confirmation: report that and do not invoke it again unless they ask.

- Safety: `safe`; side effect: `external`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `voice`, `api`.

### Input guidance

- `action.pluginId` (text, required): Plugin id
- `action.localId` (text, required): Action id
- `input` (textarea): Declared Action input

## `action.options.resolve`

**Resolve action options** — Resolve valid options for an action field, including dynamic options sources.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `voice`, `agent`, `mcp`, `api`.

### Input guidance

- `actionId` (text): Action id — Optional when optionsSourceId is provided directly.
- `fieldPath` (text): Field path — Dot-path for the action input field.
- `optionsSourceId` (text): Options source id — Direct options source lookup when known.
- `sessionId` (text): Session id — Needed for session-scoped option sources.
- `query` (text): Query filter — Optional search text to filter the returned options.
- `limit` (text): Limit — Maximum number of options to return.

## `action.spec.get`

**Get action spec** — Get one Happier action spec with input hints and examples.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `voice`, `agent`, `mcp`, `api`.

### Input guidance

- `id` (text, required): Action id — The exact Happier action id.

## `action.spec.search`

**Search action specs** — Search available Happier action specs by name, description, bindings, and field hints.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `voice`, `agent`, `mcp`, `api`.

### Input guidance

- `query` (text): Query — Natural-language search text.
- `limit` (text): Limit — Maximum number of action specs to return.

## `agents.backends.list`

**List agent backends** — List available agent backends (providers) for spawning sessions.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `voice`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `includeDisabled` (boolean): Include disabled
- `limit` (text): Max results
- `machineId` (text): Machine id (optional)

## `agents.config_options.list`

**List agent config options** — List configurable option definitions for an agent backend without exposing current values.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `voice`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `agentId` (text): Runtime agent id
- `backendTargetKey` (text): Backend target key
- `modelId` (text): Model id
- `machineId` (text): Machine id (optional)
- `limit` (text): Max results

## `agents.models.list`

**List agent models** — List available models for an agent backend.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `voice`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `agentId` (text): Runtime agent id
- `backendTargetKey` (text): Backend target key
- `machineId` (text): Machine id (optional)
- `limit` (text): Max results

## `agents.session_modes.list`

**List agent session modes** — List session modes available for an agent backend.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `voice`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `agentId` (text): Runtime agent id
- `backendTargetKey` (text): Backend target key
- `machineId` (text): Machine id (optional)
- `limit` (text): Max results

## `approval.request.create`

**Create approval request** — Create an approval request for another action to run.

- Safety: `danger`; side effect: `write`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `agent`, `mcp`, `cli`, `rpc`, `api`.

### Input guidance

- `summary` (textarea, required): Summary
- `actionId` (text, required): Action id
- `actionArgs` (textarea, required): Action args (json)

## `approval.request.decide`

**Decide approval request** — Approve or reject an approval request.

- Safety: `danger`; side effect: `write`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `mcp`, `cli`, `rpc`, `api`.

### Input guidance

- `artifactId` (text, required): Approval artifact id
- `decision` (select, required): Decision

## `approval.request.get`

**Get approval request** — Fetch one approval request by approval artifact id.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `artifactId` (text, required): Approval artifact id

## `approval.request.list`

**List approval requests** — List approval queue entries from targeted approval artifact headers.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `status` (select): Status
- `limit` (text): Limit

## `automation.conversation.admit`

**Admit Automation conversation occurrence** — Admit a conversation occurrence through the canonical Automation owner.

- Safety: `danger`; side effect: `write`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: none.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `automation.conversation.target.verify`

**Verify Automation conversation target** — Verify an Automation conversation target through the canonical Automation owner.

- Safety: `safe`; side effect: `read`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: none.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `automation.conversation.targets.list`

**List Automation conversation targets** — List current selectable Automation conversation targets through the canonical Automation owner.

- Safety: `safe`; side effect: `read`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: none.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `automation.event.admit`

**Admit Automation Event occurrence** — Read or update Event Automation state through the canonical Automation owner.

- Safety: `danger`; side effect: `write`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: none.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `automation.event.source.status.report`

**Report Automation Event source status** — Read or update Event Automation state through the canonical Automation owner.

- Safety: `danger`; side effect: `write`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: none.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `automation.event.sources.list`

**List Automation Event sources** — Read or update Event Automation state through the canonical Automation owner.

- Safety: `safe`; side effect: `read`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: none.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.automation.cancelActive`

**Cancel active browser automation** — Cancel the active browser automation operation.

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.automation.click`

**Click browser element** — Automate clicking a browser element.

- Safety: `danger`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.automation.focus`

**Focus browser element** — Automate focusing a browser element.

- Safety: `danger`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.automation.goBack`

**Automate browser back navigation** — Automate navigating back in the browser view.

- Safety: `danger`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.automation.goForward`

**Automate browser forward navigation** — Automate navigating forward in the browser view.

- Safety: `danger`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.automation.hover`

**Hover browser element** — Automate hovering over a browser element.

- Safety: `danger`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.automation.navigate`

**Automate browser navigation** — Automate navigation of the browser view to a URL.

- Safety: `danger`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.automation.press`

**Press browser key** — Automate pressing a key in the browser view.

- Safety: `danger`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.automation.queryElements`

**Query browser elements** — Query elements in the browser view by selector or role.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.automation.reload`

**Automate browser reload** — Automate reloading the current browser page.

- Safety: `danger`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.automation.scroll`

**Scroll browser surface** — Automate scrolling the browser surface.

- Safety: `danger`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.automation.select`

**Select browser option** — Automate selecting an option in the browser view.

- Safety: `danger`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.automation.semanticSnapshot`

**Capture browser semantic snapshot** — Capture a semantic accessibility snapshot of the browser view.

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.automation.setValue`

**Set browser field value** — Automate setting the value of a browser field.

- Safety: `danger`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.automation.snapshot`

**Capture browser automation snapshot** — Capture an automation snapshot of the browser view.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.automation.status`

**Get browser automation status** — Read the current browser automation status.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.automation.tap`

**Tap browser surface** — Automate tapping the browser surface.

- Safety: `danger`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.automation.timeline.get`

**Get browser automation timeline** — Read the recorded browser automation action timeline.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.automation.type`

**Type into browser** — Automate typing text into the browser view.

- Safety: `danger`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.automation.waitFor`

**Wait for browser condition** — Wait for a browser condition before continuing automation.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.context.annotation.attachComment`

**Attach browser annotation comment** — Attach a comment to the active browser annotation.

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.context.annotation.attachStroke`

**Attach browser annotation stroke** — Attach a freehand stroke to the active browser annotation.

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.context.annotation.attachStyleIntent`

**Attach browser annotation style intent** — Set the drawing style for the active browser annotation.

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.context.annotation.cancel`

**Cancel browser annotation** — Cancel the active browser annotation session.

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.context.annotation.captureElement`

**Capture browser annotation element** — Capture a selected element for the active browser annotation.

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.context.annotation.captureRegion`

**Capture browser annotation region** — Capture a cropped region for the active browser annotation.

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.context.annotation.start`

**Start browser annotation** — Start an in-page browser annotation session.

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.context.attachToAgentTurn`

**Attach browser context to agent turn** — Attach browser context to agent turn

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.context.attachToComposer`

**Attach browser context to composer** — Attach browser context to composer

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.context.captureConsoleSummary`

**Capture browser console summary** — Capture a redacted summary of recent browser console output.

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.context.captureNetworkSummary`

**Capture browser network summary** — Capture a redacted summary of recent browser network activity.

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.context.capturePage`

**Capture browser page context** — Capture the current browser page as shareable context.

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.context.captureScreenshot`

**Capture browser screenshot context** — Capture a screenshot of the browser view as context.

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.context.captureSelectedElement`

**Capture selected browser element** — Capture the selected browser element as context.

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.context.clear`

**Clear browser context** — Clear browser context

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.diagnostics.clear`

**Clear browser diagnostics** — Clear browser diagnostics

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.diagnostics.elementPicker.cancel`

**Cancel browser element picker** — Cancel browser element picker

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.diagnostics.elementPicker.start`

**Start browser element picker** — Start browser element picker

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.diagnostics.eval`

**Evaluate browser diagnostics expression** — Evaluate browser diagnostics expression

- Safety: `danger`; side effect: `danger`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.diagnostics.getProperties`

**Get browser remote object properties** — Get browser remote object properties

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.diagnostics.pause`

**Pause browser diagnostics** — Pause browser diagnostics

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.diagnostics.releaseObjectGroup`

**Release browser remote object group** — Release browser remote object group

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.diagnostics.resume`

**Resume browser diagnostics** — Resume browser diagnostics

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.diagnostics.snapshot`

**Get browser diagnostics snapshot** — Get browser diagnostics snapshot

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.goBack`

**Go back in browser** — Navigate back in a browser view history.

- Safety: `danger`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.goForward`

**Go forward in browser** — Navigate forward in a browser view history.

- Safety: `danger`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.navigate`

**Navigate browser** — Navigate a browser view to a URL.

- Safety: `danger`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.recording.attachToComposer`

**Attach browser recording to composer** — Attach a captured browser recording to the message composer.

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.recording.cancel`

**Cancel browser recording** — Cancel browser recording

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.recording.cleanupExpired`

**Clean up expired browser recordings** — Clean up expired browser recordings

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.recording.discard`

**Discard browser recording** — Discard browser recording

- Safety: `danger`; side effect: `danger`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.recording.listForView`

**List browser recordings for view** — List browser recordings for view

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.recording.start`

**Start browser recording** — Start browser recording

- Safety: `danger`; side effect: `danger`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.recording.status`

**Get browser recording status** — Get browser recording status

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.recording.stop`

**Stop browser recording** — Stop browser recording

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.reload`

**Reload browser** — Reload the current page in a browser view.

- Safety: `danger`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.stop`

**Stop browser load** — Stop the in-progress page load in a browser view.

- Safety: `danger`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.target.set`

**Set browser target** — Point a browser view at a launch target or URL.

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.view.close`

**Close browser view** — Close a browser view and detach it from its session.

- Safety: `danger`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.view.focus`

**Focus browser view** — Bring a browser view to the foreground of its session.

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `browser.view.open`

**Open browser view** — Open a browser view inside an existing browser session.

- Safety: `danger`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `bugreport.collectDiagnostics`

**Collect bug report diagnostics** — Collect bounded, redacted daemon diagnostics for a bug report.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `bugreport.getLogTail`

**Read bug report log tail** — Read a bounded daemon log tail from diagnostics-approved candidate paths.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `bugreport.uploadArtifact`

**Upload bug report artifact** — Return daemon-side bug report artifact upload availability without exposing secret material.

- Safety: `safe`; side effect: `none`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `daemon.filesystem.browseDirectory`

**Browse filesystem directory** — List a machine file-browser directory constrained to a configured browse root.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `daemon.filesystem.getDirectoryTree`

**Get directory tree** — Read a bounded directory tree through daemon filesystem path authorization.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `daemon.filesystem.listDirectory`

**List directory** — List a directory through daemon filesystem path authorization.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `daemon.filesystem.listRoots`

**List filesystem roots** — List bounded machine file-browser roots resolved from daemon filesystem access policy.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `daemon.filesystem.readFile`

**Read file** — Read a bounded file through daemon filesystem path authorization.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `daemon.filesystem.writeFile`

**Write file** — Write a bounded file through daemon filesystem path authorization.

- Safety: `danger`; side effect: `danger`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `daemon.promptAssets.delete`

**Delete prompt asset** — Delete a provider-native prompt asset through the daemon prompt asset adapter registry.

- Safety: `danger`; side effect: `danger`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `daemon.promptAssets.discover`

**Discover prompt assets** — Discover provider-native prompt assets through the daemon prompt asset adapter registry.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `daemon.promptRegistry.install`

**Install prompt registry asset** — Install a prompt registry item through bundle-capable prompt asset adapters.

- Safety: `danger`; side effect: `danger`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `daemon.promptRegistry.scanSource`

**Scan prompt registry source** — Scan a configured prompt registry source through the daemon prompt registry adapter registry.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `devices.simulator.input.button`

**Press simulator hardware button** — Press simulator hardware button

- Safety: `danger`; side effect: `external`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `devices.simulator.input.key`

**Press simulator key** — Press simulator key

- Safety: `danger`; side effect: `external`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `devices.simulator.input.pinch`

**Pinch simulator** — Pinch simulator

- Safety: `danger`; side effect: `external`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `devices.simulator.input.rotate`

**Rotate simulator** — Rotate simulator

- Safety: `danger`; side effect: `external`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `devices.simulator.input.swipe`

**Swipe simulator** — Swipe simulator

- Safety: `danger`; side effect: `external`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `devices.simulator.input.tap`

**Tap simulator** — Tap simulator

- Safety: `danger`; side effect: `external`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `devices.simulator.input.text`

**Type simulator text** — Type simulator text

- Safety: `danger`; side effect: `external`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `devices.simulator.lease.acquire`

**Acquire simulator control lease** — Acquire simulator control lease

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `devices.simulator.lease.release`

**Release simulator control lease** — Release simulator control lease

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `devices.simulator.lease.renew`

**Renew simulator control lease** — Renew simulator control lease

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `devices.simulator.list`

**List simulator devices** — List simulator devices

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `devices.simulator.sideband.request`

**Request simulator sideband data** — Request simulator sideband data

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `devices.simulator.stream.fps.set`

**Set simulator stream frame rate** — Set simulator stream frame rate

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `devices.simulator.stream.keyframe`

**Request simulator stream keyframe** — Request simulator stream keyframe

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `devices.simulator.stream.quality.set`

**Set simulator stream quality** — Set simulator stream quality

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `devices.simulator.stream.scale.set`

**Set simulator stream scale** — Set simulator stream scale

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `devices.simulator.stream.snapshot`

**Request simulator stream snapshot** — Request simulator stream snapshot

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `execution.run.action`

**Apply execution run action** — Apply an action to one execution run in the selected scope. A nonempty sessionId selects that exact authorized Session. Omitting sessionId inherits the current Action Session, or selects detached scope when none exists. sessionId:null selects detached scope even inside a current Session.

- Safety: `safe`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `ui`, `voice`, `agent`, `mcp`, `cli`, `rpc`, `api`.

### Input guidance

- `sessionId` (text): Session id — Use a nonempty value for an exact authorized Session. Omit it to inherit the current Action Session, or to select detached scope when no current Session exists. Set null to select detached scope even inside a current Session.
- `runId` (text, required): Run id
- `actionId` (text, required): Action id
- `input` (textarea): Input (JSON)

## `execution.run.ensure`

**Ensure execution run** — Ensure an existing execution run is active or resumable. A nonempty sessionId selects that exact authorized Session. Omitting sessionId inherits the current Action Session, or selects detached scope when none exists. sessionId:null selects detached scope even inside a current Session.

- Safety: `safe`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `agent`, `rpc`, `api`.

### Input guidance

- `sessionId` (text): Session id — Use a nonempty value for an exact authorized Session. Omit it to inherit the current Action Session, or to select detached scope when no current Session exists. Set null to select detached scope even inside a current Session.
- `runId` (text, required): Run id
- `resume` (boolean): Resume if needed

## `execution.run.ensure_or_start`

**Ensure or start execution run** — Ensure an existing execution run, or start a new one when no run id is supplied. A nonempty sessionId selects that exact authorized Session. Omitting sessionId inherits the current Action Session, or selects detached scope when none exists. sessionId:null selects detached scope even inside a current Session.

- Safety: `safe`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `agent`, `rpc`, `api`.

### Input guidance

- `sessionId` (text): Session id — Use a nonempty value for an exact authorized Session. Omit it to inherit the current Action Session, or to select detached scope when no current Session exists. Set null to select detached scope even inside a current Session.
- `runId` (text): Run id
- `start` (textarea): Start request (json)
- `resume` (boolean): Resume if needed

## `execution.run.get`

**Get execution run** — Get one execution run from the selected scope. A nonempty sessionId selects that exact authorized Session. Omitting sessionId inherits the current Action Session, or selects detached scope when none exists. sessionId:null selects detached scope even inside a current Session.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `voice`, `agent`, `mcp`, `cli`, `rpc`, `api`.

### Input guidance

- `sessionId` (text): Session id — Use a nonempty value for an exact authorized Session. Omit it to inherit the current Action Session, or to select detached scope when no current Session exists. Set null to select detached scope even inside a current Session.
- `runId` (text, required): Run id
- `includeStructured` (boolean): Include structured output

## `execution.run.list`

**List execution runs** — List execution runs in the selected scope. A nonempty sessionId selects that exact authorized Session. Omitting sessionId inherits the current Action Session, or selects detached scope when none exists. sessionId:null selects detached scope even inside a current Session.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `voice`, `agent`, `mcp`, `cli`, `rpc`, `api`.

### Input guidance

- `sessionId` (text): Session id — Use a nonempty value for an exact authorized Session. Omit it to inherit the current Action Session, or to select detached scope when no current Session exists. Set null to select detached scope even inside a current Session.
- `backendTarget` (text): Backend target
- `status` (select): Status
- `limit` (text): Max runs

## `execution.run.send`

**Send to execution run** — Send a message to one execution run in the selected scope. A nonempty sessionId selects that exact authorized Session. Omitting sessionId inherits the current Action Session, or selects detached scope when none exists. sessionId:null selects detached scope even inside a current Session.

- Safety: `safe`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `ui`, `voice`, `agent`, `mcp`, `cli`, `rpc`, `api`.

### Input guidance

- `sessionId` (text): Session id — Use a nonempty value for an exact authorized Session. Omit it to inherit the current Action Session, or to select detached scope when no current Session exists. Set null to select detached scope even inside a current Session.
- `runId` (text, required): Run id
- `message` (textarea, required): Message
- `resume` (boolean): Resume if needed

## `execution.run.start`

**Start execution run** — Start a new execution run. Set waitForCompletion=true to wait for a terminal run result. waitTimeoutSeconds only bounds this observation; it never stops, retries, or starts the run. A nonempty sessionId selects that exact authorized Session. Omitting sessionId inherits the current Action Session, or selects detached scope when none exists. sessionId:null selects detached scope even inside a current Session.

- Safety: `safe`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `ui`, `voice`, `agent`, `mcp`, `cli`, `rpc`, `api`.

### Input guidance

- `sessionId` (text): Session id — Use a nonempty value for an exact authorized Session. Omit it to inherit the current Action Session, or to select detached scope when no current Session exists. Set null to select detached scope even inside a current Session.
- `intent` (text, required): Intent
- `backendTarget` (textarea, required): Backend target (json)
- `instructions` (textarea): Instructions
- `permissionMode` (text, required): Permission mode
- `retentionPolicy` (text, required): Retention policy
- `runClass` (text, required): Run class
- `ioMode` (text, required): IO mode
- `waitForCompletion` (boolean): Wait for completion — Return the terminal run disposition under wait instead of returning immediately after start.
- `waitTimeoutSeconds` (text): Wait timeout seconds — Optional observation deadline; requires waitForCompletion=true and never stops the run.
- `initialContextMode` (text): Initial context mode
- `modelId` (text): Model id — Optional model for the run backend (same vocabulary as session spawn). Omit for the backend default.
- `configOptions` (json): Config options (e.g. reasoning effort) — Optional agent config-option overrides, e.g. &#123;"reasoning_effort":"high"&#125;. Merged into sessionConfigOptionOverrides at the boundary; a conflict fails with invalid_parameters.
- `connectedServices` (textarea): Connected services (json) — Optional connected-services selection for the run backend. Accepts a simple string ("&lt;service&gt;:group:&lt;id&gt;", "&lt;service&gt;:&lt;profileId&gt;", "&lt;service&gt;:native"), an array, or the full object; omitted = session-spawn defaulting (literal). Enumerate valid selections via the shared session-spawn options source.

## `execution.run.stop`

**Stop execution run** — Stop one execution run in the selected scope. A nonempty sessionId selects that exact authorized Session. Omitting sessionId inherits the current Action Session, or selects detached scope when none exists. sessionId:null selects detached scope even inside a current Session.

- Safety: `safe`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `ui`, `voice`, `agent`, `mcp`, `cli`, `rpc`, `api`.

### Input guidance

- `sessionId` (text): Session id — Use a nonempty value for an exact authorized Session. Omit it to inherit the current Action Session, or to select detached scope when no current Session exists. Set null to select detached scope even inside a current Session.
- `runId` (text, required): Run id

## `execution.run.stream.cancel`

**Cancel execution run stream** — Cancel a bounded streaming turn for an execution run. A nonempty sessionId selects that exact authorized Session. Omitting sessionId inherits the current Action Session, or selects detached scope when none exists. sessionId:null selects detached scope even inside a current Session.

- Safety: `safe`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `agent`, `rpc`, `api`.

### Input guidance

- `sessionId` (text): Session id — Use a nonempty value for an exact authorized Session. Omit it to inherit the current Action Session, or to select detached scope when no current Session exists. Set null to select detached scope even inside a current Session.
- `runId` (text, required): Run id
- `streamId` (text, required): Stream id

## `execution.run.stream.read`

**Read execution run stream** — Read bounded deltas from an execution-run stream cursor. A nonempty sessionId selects that exact authorized Session. Omitting sessionId inherits the current Action Session, or selects detached scope when none exists. sessionId:null selects detached scope even inside a current Session.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `agent`, `rpc`, `api`.

### Input guidance

- `sessionId` (text): Session id — Use a nonempty value for an exact authorized Session. Omit it to inherit the current Action Session, or to select detached scope when no current Session exists. Set null to select detached scope even inside a current Session.
- `runId` (text, required): Run id
- `streamId` (text, required): Stream id
- `cursor` (text): Cursor
- `maxEvents` (text): Max events

## `execution.run.stream.start`

**Start execution run stream** — Start a bounded streaming turn for an execution run. A nonempty sessionId selects that exact authorized Session. Omitting sessionId inherits the current Action Session, or selects detached scope when none exists. sessionId:null selects detached scope even inside a current Session.

- Safety: `safe`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `agent`, `rpc`, `api`.

### Input guidance

- `sessionId` (text): Session id — Use a nonempty value for an exact authorized Session. Omit it to inherit the current Action Session, or to select detached scope when no current Session exists. Set null to select detached scope even inside a current Session.
- `runId` (text, required): Run id
- `message` (textarea, required): Message
- `displayMessage` (textarea): Display message
- `resume` (boolean): Resume if needed

## `execution.run.wait`

**Wait for execution run** — Wait until an execution run reaches a terminal status. Pass timeoutSeconds to bound the wait; omit it for no Happier-side deadline. Timeout only ends this observation; it does not stop, retry, or start the run. Cancellation only ends this wait. A nonempty sessionId selects that exact authorized Session. Omitting sessionId inherits the current Action Session, or selects detached scope when none exists. sessionId:null selects detached scope even inside a current Session.

- Safety: `safe`; side effect: not classified.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `sessionId` (text): Session id — Use a nonempty value for an exact authorized Session. Omit it to inherit the current Action Session, or to select detached scope when no current Session exists. Set null to select detached scope even inside a current Session.
- `runId` (text, required): Run id
- `timeoutSeconds` (text): Timeout seconds (optional)
- `pollIntervalMs` (text): Poll interval (ms)

## `localServices.actions.copyUrl`

**Copy local service URL** — Copy the URL of a local service.

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `localServices.actions.forget`

**Forget local service** — Forget a detected local service.

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `localServices.actions.openPreview`

**Open local service preview** — Open a browser preview for a local service.

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `localServices.actions.restartManaged`

**Restart managed local service** — Restart a Happier-managed local service.

- Safety: `danger`; side effect: `danger`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `localServices.actions.stopManaged`

**Stop managed local service** — Stop a Happier-managed local service.

- Safety: `danger`; side effect: `danger`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `localServices.actions.terminateDetected`

**Terminate detected local service** — Terminate a detected (non-managed) local service.

- Safety: `danger`; side effect: `danger`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `localServices.inventory.list`

**List local services** — List the local services detected on a machine.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `localServices.inventory.refresh`

**Refresh local services** — Refresh the detected local services inventory.

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `localServices.launcher.history.clear`

**Clear local service launcher history** — Clear the local service launcher history.

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `localServices.launcher.openPreview`

**Open local service launcher preview** — Open a browser preview for a local service launch target.

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `localServices.launcher.registerPreview`

**Register local service launcher preview** — Register a browser preview for a local service launch target.

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `localServices.launcher.snapshot`

**Get local service launcher snapshot** — Read the current local service launcher state.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `localServices.launcher.start`

**Start local service launcher target** — Start a managed local service from a launch target.

- Safety: `danger`; side effect: `danger`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `localServices.preview.openOrCreate`

**Open or create local service preview** — Open an existing or create a new private local service preview.

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `localServices.preview.revoke`

**Revoke local service preview** — Revoke a local service preview.

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `localServices.preview.status`

**Get local service preview status** — Read the status of a local service preview.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `localServices.publicPreview.copyUrl`

**Copy public local service preview URL** — Copy the public URL of a local service exposure.

- Safety: `safe`; side effect: `external`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `localServices.publicPreview.create`

**Create public local service preview** — Create a public exposure for a local service preview (requires confirmation).

- Safety: `danger`; side effect: `danger`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `localServices.publicPreview.revoke`

**Revoke public local service preview** — Revoke a local service public exposure.

- Safety: `danger`; side effect: `danger`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `localServices.publicPreview.status`

**Get public local service preview status** — Read the status of a local service public exposure.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `machines.list`

**List machines** — List machines available on the active server scope.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `voice`, `agent`, `api`.

### Input guidance

- `limit` (text): Limit

## `memory.ensure_up_to_date`

**Ensure memory up to date** — Trigger the daemon to sync memory hints for a session (or all active sessions).

- Safety: `safe`; side effect: `write`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `voice`, `agent`, `api`.

### Input guidance

- `machineId` (text, required): Machine id
- `sessionId` (text): Session id (optional)

## `memory.get_window`

**Get memory window** — Fetch and decrypt a transcript window (used to verify/quote a memory hit).

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `voice`, `agent`, `api`.

### Input guidance

- `machineId` (text, required): Machine id
- `sessionId` (text, required): Session id
- `seqFrom` (text, required): Seq from
- `seqTo` (text, required): Seq to

## `memory.search`

**Search memory** — Search the local daemon memory index (opt-in).

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `voice`, `agent`, `api`.

### Input guidance

- `machineId` (text, required): Machine id — Machine running the daemon memory index.
- `query.query` (text, required): Query — What to search for.
- `query.mode` (select, required): Mode — Which index to search.

## `paths.list_recent`

**List recent paths** — List recent workspace directory handles (optionally filtered to a machine).

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `voice`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `machineId` (text): Machine id
- `limit` (text): Limit

## `peerMediation.observability.snapshot`

**Get peer mediation observability snapshot** — Get peer mediation observability snapshot

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `peerMediation.observability.subscribe`

**Subscribe to peer mediation observability** — Subscribe to peer mediation observability

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `peerMediation.observability.unsubscribe`

**Unsubscribe from peer mediation observability** — Unsubscribe from peer mediation observability

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugin.webhook.endpoint.checkCorrespondence`

**Check webhook endpoint correspondence** — Manage one Account-owned webhook endpoint through the canonical Webhook ingress owner.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugin.webhook.endpoint.credential.configure`

**Configure webhook credential** — Manage one Account-owned webhook endpoint through the canonical Webhook ingress owner.

- Safety: `danger`; side effect: `write`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `cli`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugin.webhook.endpoint.credential.finishRotation`

**Finish webhook credential rotation** — Manage one Account-owned webhook endpoint through the canonical Webhook ingress owner.

- Safety: `danger`; side effect: `write`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `cli`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugin.webhook.endpoint.credential.rotate`

**Rotate webhook credential** — Manage one Account-owned webhook endpoint through the canonical Webhook ingress owner.

- Safety: `danger`; side effect: `write`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `cli`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugin.webhook.endpoint.ensure`

**Ensure webhook endpoint** — Manage one Account-owned webhook endpoint through the canonical Webhook ingress owner.

- Safety: `danger`; side effect: `write`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `cli`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugin.webhook.endpoint.read`

**Read webhook endpoint** — Manage one Account-owned webhook endpoint through the canonical Webhook ingress owner.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `cli`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugin.webhook.endpoint.retarget`

**Retarget webhook endpoint** — Manage one Account-owned webhook endpoint through the canonical Webhook ingress owner.

- Safety: `danger`; side effect: `write`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `cli`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugin.webhook.endpoint.revoke`

**Revoke webhook endpoint** — Manage one Account-owned webhook endpoint through the canonical Webhook ingress owner.

- Safety: `danger`; side effect: `write`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `cli`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugins.author.build`

**Build plugin author source** — Build an external plugin-author source through the managed runtime.

- Safety: `danger`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `agent`, `mcp`, `cli`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugins.author.install`

**Prepare plugin author dependencies** — Prepare external plugin-author dependencies through the managed runtime.

- Safety: `danger`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `agent`, `mcp`, `cli`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugins.author.test`

**Test plugin author source** — Run the external plugin-author test command through the managed runtime.

- Safety: `danger`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `agent`, `mcp`, `cli`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugins.author.typecheck`

**Typecheck plugin author source** — Run the managed TypeScript check for an external plugin-author source.

- Safety: `danger`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `agent`, `mcp`, `cli`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugins.change.status`

**Get plugin change status** — Read one daemon-issued pending plugin change without creating or deciding it.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugins.dev`

**Submit plugin development snapshot** — Inspect a local plugin source and submit its current snapshot to the daemon-owned development cycle without starting a watcher.

- Safety: `danger`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `agent`, `mcp`, `cli`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugins.doctor`

**Diagnose plugin author source** — Evaluate and diagnose an external plugin-author source.

- Safety: `danger`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `agent`, `mcp`, `cli`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugins.install`

**Install plugin** — Install a local plugin source and optionally enable the dev reload loop.

- Safety: `danger`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `agent`, `mcp`, `cli`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugins.list`

**List plugins** — List installed plugins with source and load diagnostics.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugins.pack`

**Pack plugin** — Validate and package a local plugin into an installable archive.

- Safety: `danger`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `agent`, `mcp`, `cli`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugins.permissions.grants.dismissRequest`

**Dismiss plugin permission request** — Manage durable user-approved optional plugin permission grants.

- Safety: `danger`; side effect: `write`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `rpc`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugins.permissions.grants.grant`

**Grant plugin permission** — Manage durable user-approved optional plugin permission grants.

- Safety: `danger`; side effect: `write`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `rpc`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugins.permissions.grants.list`

**List plugin permission grants** — Manage durable user-approved optional plugin permission grants.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `rpc`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugins.permissions.grants.request`

**Request plugin permission grant** — Manage durable user-approved optional plugin permission grants.

- Safety: `danger`; side effect: `write`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `rpc`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugins.permissions.grants.revoke`

**Revoke plugin permission** — Manage durable user-approved optional plugin permission grants.

- Safety: `danger`; side effect: `write`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `rpc`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugins.reload`

**Reload plugin** — Reload one local development plugin through the daemon-owned plugin lifecycle.

- Safety: `danger`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `ui`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugins.scaffold`

**Scaffold plugin** — Create a local plugin scaffold from the first-party template.

- Safety: `danger`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `agent`, `mcp`, `cli`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugins.sessionHooks.disable`

**Disable Agent session hooks** — Explicitly disable ingestion for one exact Agent hook installation without deleting its owned config.

- Safety: `danger`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugins.sessionHooks.enable`

**Enable Agent session hooks** — Explicitly enable ingestion for one exact Agent hook installation.

- Safety: `danger`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugins.sessionHooks.install`

**Install Agent session hooks** — Explicitly install External Session hooks for one qualified Agent integration.

- Safety: `danger`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugins.sessionHooks.status.get`

**Get Agent session-hook status inventory** — Read a bounded, paginated inventory of portable External Session hook installation status rows.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugins.sessionHooks.uninstall`

**Uninstall Agent session hooks** — Explicitly remove only the owned entries for one exact External Session hook installation.

- Safety: `danger`; side effect: `danger`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugins.settings.get`

**Get plugin setting** — Read one declared non-secret plugin Setting from one exact scope.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `cli`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugins.settings.list`

**List plugin settings** — List declared plugin Settings for one exact Account or daemon scope.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `cli`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugins.settings.reset`

**Reset plugin setting** — Reset one declared non-secret plugin Setting to its owner default.

- Safety: `danger`; side effect: `write`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `cli`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugins.settings.secret.bind`

**Bind existing saved secret** — Bind one plugin secret to an existing SavedSecret identity without exposing its value.

- Safety: `danger`; side effect: `write`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `cli`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugins.settings.secret.delete`

**Delete plugin secret** — Delete one plugin secret through its declared custody owner without exposing its value.

- Safety: `danger`; side effect: `write`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `cli`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugins.settings.secret.status`

**Get plugin secret status** — Read safe configured status for one declared plugin secret.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `cli`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugins.settings.secret.unbind`

**Unbind plugin secret** — Remove the binding for one plugin secret without exposing its value.

- Safety: `danger`; side effect: `write`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `cli`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugins.settings.set`

**Set plugin setting** — Compare-and-set one declared non-secret plugin Setting.

- Safety: `danger`; side effect: `write`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `cli`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `plugins.uninstall`

**Uninstall plugin** — Remove a local installed plugin through the daemon-owned plugin lifecycle.

- Safety: `danger`; side effect: `danger`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `agent`, `mcp`, `cli`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `prompt_asset.export`

**Export prompt asset** — Export a prompt doc or skill bundle from the Happier library to a provider-native asset.

- Safety: `danger`; side effect: not classified.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `api`.

### Input guidance

- `artifactId` (text, required): Artifact id
- `machineId` (text, required): Machine id
- `assetTypeId` (text, required): Asset type id
- `scope` (select, required): Scope
- `directory` (text): Project directory
- `targetPath` (text): Document path
- `targetName` (text): Skill name
- `installMode` (select): Install mode

## `prompt_bundle.update`

**Update prompt bundle** — Update a skill bundle stored in the Happier prompt library.

- Safety: `danger`; side effect: not classified.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `api`.

### Input guidance

- `artifactId` (text, required): Bundle artifact id
- `title` (text, required): Title
- `skillMarkdown` (textarea, required): SKILL.md markdown
- `folderId` (text): Folder id
- `tags` (text_list): Tags

## `prompt_doc.update`

**Update prompt document** — Update a prompt document stored in the Happier prompt library.

- Safety: `danger`; side effect: not classified.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `artifactId` (text, required): Prompt artifact id
- `title` (text, required): Title
- `markdown` (textarea, required): Markdown
- `folderId` (text): Folder id
- `tags` (text_list): Tags

## `prompt_registry.install`

**Install prompt registry skill** — Import a skill bundle from a registry and optionally export it to an external skills location.

- Safety: `danger`; side effect: not classified.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `api`.

### Input guidance

- `machineId` (text, required): Machine id
- `sourceId` (text, required): Source id
- `itemId` (text, required): Item id
- `configuredSources` (textarea): Configured sources (json)
- `installTarget.assetTypeId` (text): Target asset type
- `installTarget.scope` (select): Target scope
- `installTarget.directory` (text): Project directory
- `installTarget.targetName` (text): Target skill name
- `installTarget.installMode` (select): Install mode

## `review.engines.list`

**List review engines** — List review engines currently available for the active session.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `voice`, `api`.

### Input guidance

- `sessionId` (text): Session id
- `includeDisabled` (boolean): Include disabled

## `review.start`

**Start review** — No additional description is published.

- Safety: `safe`; side effect: `external`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `ui`, `voice`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `engineIds` (multiselect, required): Review engines — Select one or more engines. Each engine runs as its own execution run.
- `instructions` (textarea, required): Instructions — What you want the reviewers to focus on.
- `changeType` (select, required): Change type — Which changes to review.
- `base.kind` (select, required): Base selection — How to define the review base for engines that need it.
- `base.baseBranch` (text): Base branch — Branch name to diff against (when base.kind=branch).
- `base.baseCommit` (text): Base commit — Commit SHA to diff against (when base.kind=commit).

## `reviews.comments.attachEvidence`

**Attach review comment evidence** — Operate on durable review comments through the shared review-comment substrate.

- Safety: `danger`; side effect: `write`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `rpc`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `reviews.comments.bulkTransition`

**Bulk transition review comments** — Operate on durable review comments through the shared review-comment substrate.

- Safety: `danger`; side effect: `write`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `rpc`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `reviews.comments.create`

**Create review comment** — Operate on durable review comments through the shared review-comment substrate.

- Safety: `danger`; side effect: `write`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `rpc`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `reviews.comments.edit`

**Edit review comment** — Operate on durable review comments through the shared review-comment substrate.

- Safety: `danger`; side effect: `write`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `rpc`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `reviews.comments.get`

**Get review comment** — Operate on durable review comments through the shared review-comment substrate.

- Safety: `safe`; side effect: `read`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `rpc`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `reviews.comments.list`

**List review comments** — Operate on durable review comments through the shared review-comment substrate.

- Safety: `safe`; side effect: `read`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `rpc`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `reviews.comments.redact`

**Redact review comment** — Operate on durable review comments through the shared review-comment substrate.

- Safety: `danger`; side effect: `write`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `rpc`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `reviews.comments.reply`

**Reply to review comment** — Operate on durable review comments through the shared review-comment substrate.

- Safety: `danger`; side effect: `write`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `rpc`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `reviews.comments.setDisposition`

**Set review comment disposition** — Operate on durable review comments through the shared review-comment substrate.

- Safety: `danger`; side effect: `write`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `rpc`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `reviews.comments.transition`

**Transition review comment** — Operate on durable review comments through the shared review-comment substrate.

- Safety: `danger`; side effect: `write`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `rpc`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `scm.diffSummary.generate`

**Generate source-control diff summary** — Generate a buffered AI summary for checkpoint-backed or working-tree source-control changes.

- Safety: `safe`; side effect: `external`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `cwd` (text, required): Repository directory
- `source.kind` (select, required): Summary source
- `turnId` (text): Turn id
- `checkpointReceiptId` (text): Checkpoint receipt id
- `modelSelector.profileId` (text): Model profile id
- `modelSelector.modelId` (text): Model id
- `modelSelector.backendTargetKey` (text): Backend target key

## `scm.hostingRepository.describePublishTargets`

**Describe hosting repository publish targets** — Describe authenticated hosting-provider targets that can receive a repository publish.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `cwd` (text, required): Repository directory
- `providerKind` (select): Hosting provider

## `scm.hostingRepository.publish`

**Publish repository to hosting provider** — Create or bind a hosting repository and publish the active source-control state.

- Safety: `danger`; side effect: `external`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `cwd` (text, required): Repository directory
- `providerKind` (select, required): Hosting provider
- `owner` (text, required): Owner
- `ownerKind` (select): Owner kind
- `repositoryName` (text, required): Repository name
- `visibility` (select, required): Visibility
- `description` (textarea): Description
- `remoteName` (text): Remote name
- `remoteUrlKind` (select): Remote URL kind
- `remoteConflictStrategy` (select): Remote conflict strategy
- `pushCurrentBranch` (boolean): Push current branch

## `scm.pullRequest.checkout`

**Check out pull request** — Check out a pull request reference in the current repository.

- Safety: `danger`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `cwd` (text): Repository directory
- `prReference.number` (text): Pull request number
- `prReference.url` (text): Pull request URL
- `prReference.headBranch` (text): Head branch

## `scm.pullRequest.get`

**Get pull request** — Read one pull request by number, URL, or head branch.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `cwd` (text): Repository directory
- `prReference.number` (text): Pull request number
- `prReference.url` (text): Pull request URL
- `prReference.headBranch` (text): Head branch

## `scm.pullRequest.list`

**List pull requests** — List pull requests for the current source-control repository.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `cwd` (text): Repository directory
- `providerId` (text): Provider id
- `base` (text): Base branch
- `head` (text): Head branch
- `state` (select): State

## `scm.pullRequest.openCompose`

**Open pull-request compose URL** — Build a provider-approved compose URL for the current branch.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `cwd` (text): Repository directory
- `providerId` (text): Provider id
- `base` (text, required): Base branch
- `head` (text, required): Head branch

## `scm.pullRequest.openOrReuse`

**Open or reuse pull request** — Create a pull request or return an existing one for the current branch.

- Safety: `danger`; side effect: `external`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `cwd` (text): Repository directory
- `providerId` (text): Provider id
- `base` (text, required): Base branch
- `head` (text): Head branch
- `title` (text): Title
- `body` (textarea): Body

## `scm.pullRequest.prepareWorktree`

**Prepare pull-request worktree** — Prepare a local worktree for a pull request reference.

- Safety: `danger`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `cwd` (text): Repository directory
- `sourcePath` (text, required): Source path
- `prReference.number` (text): Pull request number
- `prReference.url` (text): Pull request URL
- `prReference.headBranch` (text): Head branch
- `mode` (select): Mode

## `scm.pullRequest.runStacked`

**Run stacked pull-request workflow** — Run a structured branch, commit, push, and pull-request workflow.

- Safety: `danger`; side effect: `danger`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `cwd` (text): Repository directory
- `action` (select, required): Action
- `commitMessage` (textarea): Commit message
- `featureBranch` (text): Feature branch
- `filePaths` (text_list): File paths
- `base` (text): Base branch
- `head` (text): Head branch
- `title` (text): Pull request title
- `body` (textarea): Pull request body
- `defaultBranchPushPolicy` (select): Default-branch push policy

## `scm.repository.clone`

**Clone source-control repository** — Clone a hosting-provider repository into an explicitly selected local destination.

- Safety: `danger`; side effect: `external`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `provider` (textarea, required): Hosting provider (json)
- `repository` (textarea, required): Repository (json)
- `destinationParentPath` (text, required): Destination parent directory
- `destinationDirectoryName` (text, required): Destination directory name
- `protocol` (select, required): Clone protocol
- `confirmed` (boolean, required): Confirm repository clone
- `authorizationToken` (text, required): Authorization token

## `scm.repository.init`

**Initialize source-control repository** — Initialize source control for a directory through the SCM provisioning route.

- Safety: `danger`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `cwd` (text, required): Repository directory
- `initialBranch` (text): Initial branch

## `scm.repository.removeIndexLock`

**Remove stale source-control index lock** — Remove a confirmed stale Git index lock using backend-owned path resolution.

- Safety: `danger`; side effect: `danger`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `cwd` (text, required): Repository directory
- `confirmed` (boolean, required): Confirm stale index-lock removal
- `confirmationToken` (text, required): Confirmation token

## `servers.list`

**List servers** — List servers configured in the client.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `voice`, `agent`, `api`.

### Input guidance

- `limit` (text): Limit

## `session.activity.get`

**Get session activity** — Get a short activity digest for a session without transcript content.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `voice`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `sessionId` (text, required): Session id
- `windowSeconds` (text): Window seconds

## `session.archive`

**Archive session** — Archive the specified session.

- Safety: `safe`; side effect: not classified.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `sessionId` (text, required): Session id

## `session.checkpoint`

**Create checkpoint** — Create a source-qualified checkpoint for the selected session.

- Safety: `danger`; side effect: not classified.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `ui`, `rpc`, `api`.

### Input guidance

- `sessionId` (text, required): Session id
- `scopes` (multiselect, required): Scopes

## `session.checkpoint_code_rollback`

**Rollback code to checkpoint** — Apply a checkpoint-backed code rollback for the selected session turn.

- Safety: `danger`; side effect: not classified.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `ui`, `rpc`, `api`.

### Input guidance

- `sessionId` (text, required): Session id
- `turnId` (text, required): Turn id
- `cwd` (text, required): Working directory

## `session.continue_with_replay`

**Continue session with replay** — Create a continuation session from a replay seed while preserving the existing RPC wire contract.

- Safety: `safe`; side effect: not classified.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `directory` (text, required): Directory
- `backendTarget` (text, required): Backend target
- `replay` (textarea, required): Replay seed

## `session.events.get`

**Get session events** — Inspect raw session events (tool calls, tool results, token counts, lifecycle, permission, stream, session events) for diagnostics. Use session_transcript_get for normal transcript reading.

- Safety: `safe`; side effect: not classified.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `sessionId` (text, required): Session id
- `limit` (text): Limit
- `cursor` (text): Cursor

## `session.fork`

**Fork session** — Create a new session from the latest state of the selected session.

- Safety: `safe`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `ui`, `voice`, `rpc`, `api`.

### Input guidance

- `sessionId` (text): Session id

## `session.goal.clear`

**Clear session goal** — Clear the native session goal.

- Safety: `safe`; side effect: not classified.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `sessionId` (text, required): Session id

## `session.goal.get`

**Get session goal** — Get the editable session goal when the provider supports native goals.

- Safety: `safe`; side effect: not classified.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `sessionId` (text, required): Session id

## `session.goal.set`

**Set session goal** — Set or update the native session goal.

- Safety: `safe`; side effect: not classified.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `sessionId` (text, required): Session id
- `objective` (textarea): Objective
- `status` (text): Status
- `tokenBudget` (text): Token budget

## `session.handoff`

**Hand off session** — Move the current session to another machine while keeping the same session id.

- Safety: `safe`; side effect: not classified.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `ui`, `rpc`, `api`.

### Input guidance

- `sessionId` (text): Session id
- `targetMachineId` (text): Target machine id

## `session.handoff.status.get`

**Get session handoff status** — Read scalar status for an in-progress session handoff.

- Safety: `safe`; side effect: not classified.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `handoffId` (text, required): Handoff id

## `session.history.get`

**Get session history** — DEPRECATED: use session_events_get. Returns diagnostic session events with cleaner pagination.

- Safety: `safe`; side effect: not classified.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `sessionId` (text, required): Session id
- `limit` (text): Limit
- `format` (select): Format
- `includeMeta` (boolean): Include meta
- `includeStructuredPayload` (boolean): Include structured payload

## `session.list`

**List sessions** — List recent sessions the user can target.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `voice`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `limit` (text): Limit
- `cursor` (text): Cursor
- `includeLastMessagePreview` (boolean): Include last message preview

## `session.log.tail`

**Tail session log** — Read a bounded byte tail from an allowed session log file.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `path` (text, required): Log path
- `maxBytes` (text): Maximum bytes
- `offset` (text): File offset

## `session.message.send`

**Send a message to a session** — Send a user message to the AI coding assistant inside the specified session.

- Safety: `safe`; side effect: `external`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `ui`, `voice`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `sessionId` (text): Session id
- `message` (textarea, required): Message
- `permissionModeOverride` (text): Permission mode override (optional)
- `modelOverride` (text): Model override (optional)
- `providerConnectionId` (text): Provider connection id (optional)
- `wait` (boolean): Wait for idle (optional)
- `timeoutSeconds` (text): Timeout seconds (optional)

## `session.messages.recent.get`

**Get recent messages** — DEPRECATED: use session_transcript_get. Returns semantic transcript items with cleaner pagination.

- Safety: `safe`; side effect: not classified.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `sessionId` (text, required): Session id
- `limit` (text): Limit
- `cursor` (text): Cursor
- `includeUser` (boolean): Include user
- `includeAssistant` (boolean): Include assistant

## `session.mode.set`

**Set session mode** — Request a new ACP session mode for the current session when the active provider supports session modes.

- Safety: `safe`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `ui`, `voice`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `sessionId` (text): Session id — Optional when the active target session is already correct.
- `modeId` (select, required): Mode id — Use default to clear the override and return to the provider default mode.

## `session.model.set`

**Set session model** — Set the model override for the specified session.

- Safety: `safe`; side effect: not classified.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `sessionId` (text, required): Session id
- `modelId` (text, required): Model id
- `providerConnectionId` (text): Provider connection id

## `session.open`

**Open session** — No additional description is published.

- Safety: `safe`; side effect: `external`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `ui`, `voice`, `api`.

### Input guidance

- `sessionId` (text): Session id
- `sessionTitle` (text): Session title

## `session.pendingInput.interruptAndRun`

**Interrupt and run now** — Interrupt the live provider turn so its exact native queued prompt can run now.

- Safety: `danger`; side effect: `danger`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `cli`, `rpc`, `api`.

### Input guidance

- `sessionId` (text, required): Session id
- `localId` (text, required): Pending message local id
- `expectedStateAtMs` (text): Expected state timestamp

## `session.permission.remote.grants.list`

**List remotely mediated permission grants** — List source-scoped remote permission grants visible to the authenticated caller.

- Safety: `safe`; side effect: `read`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `cli`, `rpc`, `api`.

### Input guidance

- `sessionId` (text, required): Session id
- `limit` (text): Maximum grants
- `cursor` (text): Continuation cursor

## `session.permission.remote.grants.revoke`

**Revoke a remotely mediated permission grant** — Revoke one source-scoped remote permission grant.

- Safety: `safe`; side effect: `write`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `cli`, `rpc`, `api`.

### Input guidance

- `sessionId` (text, required): Session id
- `turnId` (text, required): Turn id
- `requestId` (text, required): Request id
- `grantId` (text, required): Grant id

## `session.permission.remote.pending.list`

**List remotely mediated permission requests** — List the caller mediator’s current permission requests for one exact source authority.

- Safety: `safe`; side effect: `read`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: none.

### Input guidance

- `sessionId` (text, required): Session id
- `sourceRef` (text, required): Source reference
- `sourceRevisionOrEpoch` (text, required): Source revision

## `session.permission.remote.respond`

**Respond to a remotely mediated permission request** — Submit an attributed external-human decision for one current permission request.

- Safety: `safe`; side effect: `write`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: none.

### Input guidance

- `sessionId` (text, required): Session id
- `turnId` (text, required): Turn id
- `requestId` (text, required): Request id
- `sourceRef` (text, required): Source reference
- `sourceRevisionOrEpoch` (text, required): Source revision
- `idempotencyKey` (text, required): Idempotency key
- `actor.namespace` (text, required): External principal namespace
- `actor.principalId` (text, required): External principal id
- `decision` (select, required): Decision
- `scope` (select, required): Scope

## `session.permission_mode.set`

**Set session permission mode** — Update the permission intent (read_only/workspace_write/etc) for the specified session.

- Safety: `safe`; side effect: not classified.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `agent`, `mcp`, `cli`, `rpc`, `api`.

### Input guidance

- `sessionId` (text, required): Session id
- `permissionMode` (text, required): Permission mode

## `session.restore`

**Restore checkpoint** — Restore a selected session checkpoint source.

- Safety: `danger`; side effect: not classified.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `ui`, `rpc`, `api`.

### Input guidance

- `sessionId` (text, required): Session id
- `scopes` (multiselect, required): Scopes
- `candidate.source` (select, required): Source

## `session.rollback`

**Rollback conversation** — Roll back conversation state in the selected session.

- Safety: `danger`; side effect: not classified.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `ui`, `rpc`, `api`.

### Input guidance

- `sessionId` (text): Session id

## `session.skill_catalog.list`

**List session skills** — List provider-visible skills available to the session.

- Safety: `safe`; side effect: not classified.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `sessionId` (text, required): Session id
- `cwd` (text): Working directory

## `session.spawn_new`

**Create session** — No additional description is published.

- Safety: `safe`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `ui`, `voice`, `agent`, `mcp`, `cli`, `rpc`, `api`.

### Input guidance

- `creationKey` (text): Creation key
- `executionTarget.serverId` (text, required): Server id
- `executionTarget.machineId` (text, required): Machine id
- `directory` (text, required): Directory
- `organizationPlacement` (json): Organization placement
- `agentTarget` (json, required): Agent target
- `modelSelection` (json): Model selection
- `title` (text): Title
- `permissionMode` (text): Permission mode
- `agentModeId` (text): Agent mode
- `configuration` (json): Configuration
- `profileId` (text): Profile id
- `connectedServices` (json): Connected services
- `mcpSelection` (json): MCP selection
- `transcriptStorage` (text): Transcript storage
- `terminal` (json): Terminal
- `checkoutCreationDraft` (json): Checkout creation
- `initialMessage` (textarea): Initial message
- `agentSessionStartupInstructionsV1` (json): Startup instructions

## `session.status.get`

**Get session status** — Get summary status for a session, optionally refreshing live agent state.

- Safety: `safe`; side effect: not classified.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `sessionId` (text, required): Session id
- `live` (boolean): Live

## `session.stop`

**Stop session** — Request that the local daemon stops the specified session.

- Safety: `safe`; side effect: not classified.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `agent`, `mcp`, `cli`, `rpc`, `api`.

### Input guidance

- `sessionId` (text, required): Session id

## `session.target.primary.set`

**Set primary action session** — Set which session the voice assistant should target by default.

- Safety: `safe`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `ui`, `voice`, `mcp`, `api`.

### Input guidance

- `sessionId` (text): Session id (or null)
- `sessionTitle` (text): Session title

## `session.target.tracked.set`

**Set tracked sessions** — Set which sessions should be treated as tracked for updates/snippets.

- Safety: `safe`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `ui`, `voice`, `api`.

### Input guidance

- `sessionIds` (text_list, required): Session ids

## `session.terminalComposer.clear`

**Clear terminal composer** — Clear the pending terminal composer draft for a session runtime.

- Safety: `danger`; side effect: `danger`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `mcp`, `cli`, `rpc`, `api`.

### Input guidance

- `sessionId` (text, required): Session id
- `expectedStateAtMs` (text): Expected state timestamp

## `session.title.set`

**Set session title** — Set the title (summary text) shown for a session.

- Safety: `safe`; side effect: not classified.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `sessionId` (text): Session id
- `title` (text, required): Title

## `session.transcript.get`

**Get session transcript** — Read the semantic transcript for a session as clean user/assistant messages with optional tool/reasoning/event flags.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `voice`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `sessionId` (text, required): Session id
- `limit` (text): Limit
- `cursor` (text): Cursor
- `direction` (select): Direction — Page away from the cursor: before reads older items (the default), after reads newer ones.
- `maxCharsPerMessage` (text): Message truncation chars — Optional per-message truncation budget. Omit or pass null for full message text.

## `session.unarchive`

**Unarchive session** — Unarchive the specified session.

- Safety: `safe`; side effect: not classified.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `sessionId` (text, required): Session id

## `session.usageLimit.checkNow`

**Check usage-limit recovery now** — Ask the session runtime to perform a safe provider-owned usage-limit recovery check.

- Safety: `safe`; side effect: not classified.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `sessionId` (text, required): Session id
- `provider` (text): Provider — Optional provider id for provider-scoped recovery controls.
- `operation` (select): Operation
- `resumePromptMode` (select): Resume prompt mode

## `session.usageLimit.consumeResetCredit`

**Apply usage-limit reset credit** — Ask the session runtime to spend a connected-service reset credit for usage-limit recovery.

- Safety: `danger`; side effect: not classified.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `sessionId` (text, required): Session id
- `provider` (text): Provider — Optional provider id for provider-scoped recovery controls.
- `resumePromptMode` (select): Resume prompt mode

## `session.usageLimit.waitResume.cancel`

**Cancel usage-limit wait resume** — Cancel the active usage-limit wait/resume intent for a session.

- Safety: `safe`; side effect: not classified.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `sessionId` (text, required): Session id
- `issueFingerprint` (text): Issue fingerprint
- `armedAtMs` (number): Armed at (ms)
- `runtimeAuthRecoveryAttemptId` (text): Runtime auth recovery attempt id

## `session.usageLimit.waitResume.enable`

**Enable usage-limit wait resume** — Arm a durable intent to continue a session when a provider usage limit is lifted.

- Safety: `safe`; side effect: not classified.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `sessionId` (text, required): Session id
- `issueFingerprint` (text): Issue fingerprint
- `remember` (boolean): Remember
- `resumePromptMode` (select): Resume prompt mode

## `session.user_action.answer`

**Respond to user-action request** — Approve, reject, request changes, or provide structured answers for an active user-action request.

- Safety: `safe`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `ui`, `voice`, `agent`, `mcp`, `cli`, `rpc`, `api`.

### Input guidance

- `sessionId` (text): Session id
- `requestId` (text): Request id
- `decision` (select): Decision — Use approve or reject for general user actions, or request_changes when you need the coding assistant to revise something first.
- `reason` (textarea): Reason — Required when requesting changes. Optional extra context for a rejection.
- `answers` (json): Answers — Structured answers for question-style user-action requests such as AskUserQuestion.

## `session.vendor_plugin_catalog.list`

**List session vendor plugins** — List provider-owned vendor plugins available to the session.

- Safety: `safe`; side effect: not classified.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `sessionId` (text, required): Session id
- `cwd` (text): Working directory

## `session.wait.idle`

**Wait for session idle** — Wait until the session becomes idle or the timeout elapses.

- Safety: `safe`; side effect: not classified.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `sessionId` (text, required): Session id
- `timeoutSeconds` (text): Timeout seconds

## `session.work_state.get`

**Get session work state** — Get the normalized current goal, task, and todo snapshot for a session.

- Safety: `safe`; side effect: not classified.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `sessionId` (text, required): Session id

## `sessions.external.backgroundFollow.set`

**Set external session follow policy** — Enable or disable background following for an external session.

- Safety: `danger`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `sessionId` (text, required): Linked session id
- `enabled` (boolean, required): Enabled

## `sessions.external.follow`

**Follow external session lease** — Attach an ephemeral follow lease to an external session link.

- Safety: `danger`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `sessionId` (text, required): Linked session id
- `leaseId` (text): Lease id
- `ttlMs` (text): Lease TTL milliseconds

## `sessions.external.materialize.start`

**Materialize external session** — Start or converge on the canonical external-session materialization operation.

- Safety: `danger`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `rpc`.

### Input guidance

- `request` (textarea, required): Operation request

## `sessions.external.operation.cancel`

**Cancel external session operation** — Record an explicit cancellation intent for the current operation revision.

- Safety: `danger`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `sessionId` (text, required): Session id
- `operationId` (text, required): Operation id
- `revision` (text, required): Revision

## `sessions.external.operation.discard`

**Discard external session operation** — Destructively discard an eligible initial partial operation and its private staging.

- Safety: `danger`; side effect: `danger`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `sessionId` (text, required): Session id
- `operationId` (text, required): Operation id
- `revision` (text, required): Revision

## `sessions.external.operation.resume`

**Resume external session operation** — Explicitly resume a passively hydrated operation from its durable checkpoint.

- Safety: `danger`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `sessionId` (text, required): Session id
- `operationId` (text, required): Operation id
- `revision` (text, required): Revision

## `sessions.external.operation.retry`

**Retry external session operation** — Explicitly retry the canonical recovery phase at the current revision.

- Safety: `danger`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `sessionId` (text, required): Session id
- `operationId` (text, required): Operation id
- `revision` (text, required): Revision

## `sessions.external.operation.status.get`

**Get external session operation status** — Passively read the current canonical external-session operation projection.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `sessionId` (text, required): Session id
- `operationId` (text, required): Operation id
- `revision` (text, required): Revision

## `sessions.external.status.get`

**Get external session status** — Read bounded status and takeover readiness for a linked external session.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `sessionId` (text, required): Linked session id

## `sessions.external.unfollow`

**Unfollow external session lease** — Detach an external-session follow lease.

- Safety: `danger`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `sessionId` (text, required): Linked session id
- `leaseId` (text, required): Lease id

## `sessions.spawn.connected_services.list`

**List spawn connected services** — List connected-service references available for new sessions without exposing credentials.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `voice`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `agentId` (text): Runtime agent id
- `backendTargetKey` (text): Backend target key
- `includeUnavailable` (boolean): Include unavailable

## `sessions.spawn.mcp_servers.preview`

**Preview spawn MCP servers** — Preview MCP servers that would be available to a new session.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `voice`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `agentId` (text): Runtime agent id
- `backendTargetKey` (text): Backend target key
- `machineId` (text): Machine id
- `directory` (text): Directory
- `path` (text): Path
- `selection` (json): Selection
- `limit` (text): Max results

## `sessions.spawn.profiles.list`

**List spawn profiles** — List backend profile references available for new sessions without exposing secret bindings.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `ui`, `voice`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `agentId` (text): Runtime agent id
- `backendTargetKey` (text): Backend target key
- `limit` (text): Max results

## `sessions.subagents.get`

**Get session subagent** — Return one provider-neutral subagent projection by id.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `id` (text, required): Subagent id
- `parentSessionId` (text): Parent session id

## `sessions.subagents.list`

**List session subagents** — Return bounded provider-neutral subagent projections for a session.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `agent`, `rpc`, `api`.

### Input guidance

- `parentSessionId` (text): Parent session id
- `groupId` (text): Group id
- `limit` (text): Limit

## `sessions.subagents.watch`

**Watch session subagents** — Register the bounded host subagent watcher path and return its initial typed projection snapshot.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `parentSessionId` (text): Parent session id
- `id` (text): Subagent id

## `subagents.delegate.start`

**Start delegate run** — No additional description is published.

- Safety: `safe`; side effect: `external`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `ui`, `voice`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `backendTargetKeys` (multiselect, required): Provider/backend targets — Select provider/backend targets for Happier-managed runs; use repeated launches or provider-native subagents for homogeneous parallelism capacity.
- `instructions` (textarea, required): Instructions — What you want the delegate(s) to do.
- `permissionMode` (select): Permission mode — Permission mode. Allowed values: read_only | default | workspace_write | yolo.
- `modelId` (text): Model id — Optional model applied to every started run (same vocabulary as session spawn). Omit for the backend default.
- `configOptions` (json): Config options (e.g. reasoning effort) — Optional agent config-option overrides applied to every started run, e.g. &#123;"reasoning_effort":"high"&#125;. Merged canonically; a conflict with sessionConfigOptionOverrides fails.
- `connectedServicesByBackendTargetKey` (textarea): Connected services per target (json) — Optional connected-services selection per backend target key. Accepts a simple string ("&lt;service&gt;:group:&lt;id&gt;", "&lt;service&gt;:&lt;profileId&gt;", "&lt;service&gt;:native"), an array, or the full object; omitted targets use session-spawn defaulting (literal). Enumerate valid selections via the shared session-spawn options source.

## `subagents.plan.start`

**Start plan run** — No additional description is published.

- Safety: `safe`; side effect: `external`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `ui`, `voice`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `backendTargetKeys` (multiselect, required): Provider/backend targets — Select provider/backend targets for Happier-managed runs; use repeated launches or provider-native subagents for homogeneous parallelism capacity.
- `instructions` (textarea, required): Instructions — What you want the planner(s) to do.
- `modelId` (text): Model id — Optional model applied to every started run (same vocabulary as session spawn). Omit for the backend default.
- `configOptions` (json): Config options (e.g. reasoning effort) — Optional agent config-option overrides applied to every started run, e.g. &#123;"reasoning_effort":"high"&#125;. Merged canonically; a conflict with sessionConfigOptionOverrides fails.
- `connectedServicesByBackendTargetKey` (textarea): Connected services per target (json) — Optional connected-services selection per backend target key. Accepts a simple string ("&lt;service&gt;:group:&lt;id&gt;", "&lt;service&gt;:&lt;profileId&gt;", "&lt;service&gt;:native"), an array, or the full object; omitted targets use session-spawn defaulting (literal). Enumerate valid selections via the shared session-spawn options source.

## `transcript.follow`

**Follow session transcript** — Create or refresh a retained bounded transcript follow lease.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `sessionId` (text): Session id
- `cursor` (text, required): Cursor
- `leaseId` (text): Lease id
- `maxBytes` (text): Maximum bytes
- `maxItems` (text): Maximum items
- `idleTtlMs` (text): Idle TTL milliseconds

## `transcript.import`

**Import session transcript rows** — Import a bounded batch of transcript rows through the session transcript writer owner.

- Safety: `danger`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `sessionId` (text): Session id
- `importId` (text): Import id
- `items` (textarea, required): Transcript rows
- `maxItems` (text): Maximum items

## `transcript.page`

**Page session transcript** — Read a bounded older transcript page using cursor-backed storage.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `sessionId` (text): Session id
- `cursor` (text): Cursor
- `maxBytes` (text): Maximum bytes
- `maxItems` (text): Maximum items

## `transcript.readAfter`

**Read session transcript after cursor** — Read bounded transcript deltas after a cursor.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `sessionId` (text): Session id
- `cursor` (text, required): Cursor
- `maxBytes` (text): Maximum bytes
- `maxItems` (text): Maximum items

## `transcript.search`

**Search session transcript** — Search transcript rows through bounded forward cursor reads.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `sessionId` (text): Session id
- `query` (text, required): Query
- `cursor` (text): Cursor
- `maxBytes` (text): Maximum bytes
- `maxItems` (text): Maximum items
- `maxReads` (text): Maximum reads

## `transcript.unfollow`

**Unfollow session transcript** — Release a retained transcript follow lease.

- Safety: `safe`; side effect: `write`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `rpc`, `api`.

### Input guidance

- `sessionId` (text): Session id
- `leaseId` (text, required): Lease id

## `ui.current_context.command.invoke`

**Invoke current UI command** — Invoke one currently available opaque command from the local current UI context. If the result is \`denied\`, the person declined the confirmation: report that and do not invoke it again unless they ask.

- Safety: `safe`; side effect: `external`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `voice`, `api`.

### Input guidance

- `commandId` (text, required): Opaque command id

## `ui.current_context.read`

**Read current UI context** — Read the current local UI navigation context and its bounded opaque command descriptors.

- Safety: `safe`; side effect: `read`.
- Approval: `required` result; `blocking` flow.
- Also surfaced on: `voice`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `ui.pet.choose`

**Choose pet** — Open pet settings so the user can choose or manage their companion.

- Safety: `safe`; side effect: not classified.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `ui.voice_agent.teleport`

**Teleport voice agent to session root** — Move the daemon-backed voice agent into the current or specified session root.

- Safety: `safe`; side effect: `write`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `ui`, `voice`, `api`.

### Input guidance

- `sessionId` (text): Session id

## `ui.voice_global.reset`

**Reset voice agent** — No additional description is published.

- Safety: `safe`; side effect: `write`.
- Approval: `none` result; `deferred` flow.
- Also surfaced on: `ui`, `voice`, `api`.

### Input guidance

No field hints are published. Use the typed input accepted by `execute`.

## `voice_agent.start`

**Start voice agent run** — No additional description is published.

- Safety: `safe`; side effect: `external`.
- Approval: `optional` result; `deferred` flow.
- Also surfaced on: `ui`, `voice`, `agent`, `mcp`, `cli`, `api`.

### Input guidance

- `backendTargetKeys` (multiselect, required): Provider/backend targets — Select provider/backend targets for the Happier-managed voice agent run; this is not parallelism capacity.
- `instructions` (textarea, required): Instructions — Initial instructions for the voice agent run.
