---
title: Protocol
description: Shared contracts between server, CLI, and app.
---

The repo includes a shared protocol package for types and runtime schemas:

- `packages/protocol`

Keep it as the source of truth for request/response shapes where possible.

