---
title: Pets
description: Enable Happier pet companions, choose built-in or Codex-compatible pets, and understand how they work with the app and desktop activity overlay.
---

Happier pets are optional animated companions for the authenticated app experience. They can sit in the web app shell and, on desktop, join the native activity overlay that appears outside the main window.

Pets are a user opt-in feature. A server can allow the feature while each person still keeps pets disabled until they turn them on from **Settings -> Pets**.

## What pets do

Pets make session activity easier to notice without replacing the normal session list, inbox, or notifications.

- They idle quietly when nothing needs attention.
- They react to clicks with a short jump.
- They occasionally perform small idle actions instead of animating constantly.
- They switch animation state when a session is running, waiting for you, failed, or ready for review.
- On desktop, they can appear inside the activity overlay beside session cards and quick actions.

Pets never approve permissions or send messages by themselves. They only open the relevant UI or send a quick reply after you type and submit it.

## Enable pets

Open **Settings -> Pets**.

1. Turn on **Enable pets** for your account.
2. Choose a built-in pet or import one from a connected machine.
3. Adjust **Companion size** if you want the pet smaller or larger.
4. On desktop, configure how the companion participates in the desktop activity overlay.

The account setting is off by default. Device overrides let one device inherit, force-enable, or force-disable pets without changing the account preference.

Pets only render inside signed-in app surfaces. They do not appear on sign-in, onboarding, or other public routes.

## Built-in pets

Happier ships with built-in pets that are available without importing anything:

- Blink
- Fury
- Milo
- Oli
- Titi

The built-in pets appear as selectable tiles in **Settings -> Pets**. Selecting one updates the current account default unless the device is using a local-only override.

## Companion size

The **Companion size** slider is device-local and applies to pet previews, the web app-shell companion, and the desktop companion.

The available range is 75% to 150%. The pet keeps the Codex-compatible sprite aspect ratio while resizing.

## Import Codex-compatible pets

Happier can detect Codex-compatible pet packages from connected machines.

Use **Detect Codex pets** in **Settings -> Pets** to scan the active daemon machine. Detection is explicit: Happier does not scan local Codex homes until you request it or trigger the refresh command.

Detected pets can be:

- used from the detected Codex home,
- imported into Happier as a managed local pet,
- imported into the account library when pet sync is enabled,
- removed from the device list later.

Local pets stay on the current machine unless you explicitly import them into the account library.

## Package format

A pet package is a directory with this shape:

```text
<pet-id>/
  pet.json
  spritesheet.webp
```

`spritesheet.png` is also accepted. The manifest must use one of those literal file names.

`pet.json` has this format:

```json
{
  "id": "milo",
  "displayName": "Milo",
  "description": "A compact tabby companion.",
  "spritesheetPath": "spritesheet.webp"
}
```

The V1 spritesheet is Codex-compatible:

| Property | Value |
| --- | --- |
| Size | `1536x1872` |
| Grid | 8 columns by 9 rows |
| Frame size | `192x208` |
| Supported formats | `image/webp`, `image/png` |

Rows map to these states:

| Row | State |
| --- | --- |
| 0 | `idle` |
| 1 | `running-right` |
| 2 | `running-left` |
| 3 | `waving` |
| 4 | `jumping` |
| 5 | `failed` |
| 6 | `waiting` |
| 7 | `running` |
| 8 | `review` |

Happier validates the manifest, media type, dimensions, package size, and path safety before importing or syncing a custom pet.

## Desktop activity overlay

On Tauri desktop builds, pets integrate with the native desktop activity overlay.

The desktop activity overlay can:

- show the selected pet outside the main app window,
- avoid rendering a duplicate in-app web pet while the desktop overlay owns the companion,
- show compact or expanded activity cards for sessions that need attention,
- open the relevant session in the main app,
- send quick replies from overlay controls,
- reset the pet/overlay position from settings.

Pet settings control whether the pet companion participates in the overlay. Desktop settings control the broader activity overlay host: visibility, attention filters, auto-hide, presentation mode, placement, drag repositioning, and preview text.

Pet overlay visibility modes:

| Mode | Behavior |
| --- | --- |
| Always when enabled | Let the pet make the desktop overlay visible whenever pets are enabled. |
| Attention or active | Let the pet make the overlay visible for active sessions or attention states. |
| Attention only | Let the pet make the overlay visible for waiting, failed, or ready states. |

The desktop activity overlay may also become visible because of non-pet activity settings, such as permission requests or session activity filters.

## Activity states

The companion chooses its animation state from session activity.

Activity is prioritized as:

1. waiting for you
2. failed
3. ready for review
4. running
5. idle

Desktop overlay cards can open the session, show direct actions such as permission or question responses, and provide quick reply controls when the active overlay state supports them.

## Sync and privacy

Pet companion display and pet account sync are separate server features.

- `pets.companion` controls whether the pet companion feature is available at all.
- `pets.sync` controls account pet library sync.

The server allows `pets.companion` by default so users can see the setting, but the account-level pet setting is off by default. `pets.sync` is off by default unless the server enables it.

When sync is disabled, built-in and local managed pets still work on the device that owns them. Account-library pet import and account pet selection require `pets.sync`.

Synced settings and pet records store sanitized references, metadata, digests, and size information. They do not store raw `data:` URIs, local filesystem paths, private object keys supplied by a client, or spritesheet bytes in settings.

## Server administration

Self-hosted and managed deployments can disable pet features through server feature flags.

```bash
HAPPIER_FEATURE_PETS_COMPANION__ENABLED=0
HAPPIER_FEATURE_PETS_SYNC__ENABLED=0
```

When `pets.companion` is disabled by the server:

- clients hide or disable the pet companion surfaces,
- **Settings -> Pets** shows a disabled notice instead of the pet library,
- daemon pet RPCs return a stable feature-disabled response,
- custom pet import, discovery, preview, and removal actions are blocked.

When only `pets.sync` is disabled:

- local and built-in pets still work,
- account-library import and synced account pet selection are unavailable.

## Troubleshooting

### The pet does not appear

Check these in order:

1. You are signed in and inside the app shell.
2. The selected server allows `pets.companion`.
3. **Settings -> Pets -> Enable pets** is on.
4. This device has not forced the pet setting to **Disabled**.
5. On desktop, the desktop activity overlay is enabled or the web app-shell pet is allowed to render.

### Codex pets are not detected

Make sure the active daemon machine is online, then press **Detect Codex pets** again.

The package must contain a valid `pet.json` and a literal `spritesheet.webp` or `spritesheet.png` file inside the package directory.

### Imported local pets disappear on another device

Local imports are intentionally device-local. Import the pet into the account library if your server enables `pets.sync` and you want to use it across devices.

## Related pages

- [Server feature flags](/features/feature-flags)
- [Notifications](/advanced/notifications)
- [Codex provider](/providers/codex)
