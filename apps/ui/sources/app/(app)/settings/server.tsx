import * as React from 'react';

import { ServerSettingsScreen } from '@/components/settings/server/screens/ServerSettingsScreen';

export default React.memo(function ServerSettingsRoute() {
    return <ServerSettingsScreen />;
});
