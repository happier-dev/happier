
# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
app.local.js

# Native projects are generated by `expo prebuild` (do not commit).
/ios/
/android/
!sources/activity/adapters/ios/
!sources/activity/adapters/ios/**
# @end expo-cli

# Local Expo module native sources are committed, but their build outputs are not.
modules/*/android/.cxx/
modules/*/android/build/
modules/*/ios/build/

node_modules/
.expo/
dist/
web-build/

# Vendored by postinstall for Monaco web editor surface (do not commit).
public/monaco/

# Vendored by postinstall for optional web-only runtimes (do not commit).
public/vendor/*
!public/vendor/kokoro/
public/vendor/kokoro/*
!public/vendor/kokoro/kokoroTtsWorker.js

# Vendored by postinstall for Pierre diffs worker (do not commit).
public/pierre-diff-worker*.js

# App Store Connect API keys (generated by local pipeline / CI; do not commit).
.eas/keys/

.env.local

# Generated by local Vitest shard helpers (not a source file).
vitest-files.json
shard*-files.txt

# Generated by tauri-build when command ACLs are synthesized from the app manifest.
src-tauri/permissions/autogenerated/

# Stray hand-written/transpiled JS shadows the canonical .ts in Metro/Vitest
# resolution (.js resolves before .ts). Never commit JS next to TS sources.
sources/*.js
