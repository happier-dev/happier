# Native EAS builds do not need web-only vendored assets.
public/monaco/
public/vendor/

# Local env files should never be uploaded to EAS build workers.
# Use EAS project env vars or `eas.json` `build.<profile>.env` instead.
.env
.env*.local
