# Archived: PI and ACP Agent Runtime Feature Matrix

> **Historical snapshot — not current implementation guidance.** This matrix predates the completed Agent/Provider vocabulary split. In this file, “provider” usually means an executable Agent, not a first-class model Provider, and several paths/owners have since been removed or moved. Use [Agents catalog](./agents-catalog.md) for executable Agent ownership and [Providers](./providers.md) for model-source architecture.

This file must not be used to locate current owners or plan new changes. Re-derive any historical claim from the current plugin contribution, Agent catalog, and Provider contracts before acting on it.

Legend:

- `native`: provider keeps a provider-owned runtime/control plane
- `bespoke ACP`: provider uses ACP, but still owns backend/runtime glue
- `catalog+hook`: provider can ride the generic ACP catalog path plus a thin hook layer
- `missing`: no shipped surface today

Common cross-cutting files:

- Catalog/runtime spine: `apps/cli/src/agent/acp/runtime/createCatalogProviderAcpRuntime.ts`, `apps/cli/src/agent/acp/createCatalogAcpBackend.ts`
- Built-in catalog-defined ACP path: `apps/cli/src/agent/acp/catalog/createCatalogDefinedAcpEntry.ts`, `apps/cli/src/agent/acp/catalog/createCatalogDefinedAcpBackend.ts`, `apps/cli/src/agent/acp/catalog/runCatalogDefinedAcpAgent.ts`
- Built-in ACP metadata: `packages/agents/src/acp.ts`, `packages/agents/src/manifest.ts`, `packages/agents/src/auth.ts`, `packages/agents/src/cli/runtime.ts`
- Backend selection in UI: `apps/ui/sources/agents/backendCatalog/getResolvedBackendCatalogEntries.ts`, `apps/ui/sources/components/sessions/new/hooks/useNewSessionScreenModel.tsx`, `apps/ui/sources/__tests__/routes/(app)/session/[id]/runs/new.test.tsx`
- Execution-run registry: `apps/cli/src/agent/executionRuns/registry/executionRunBackendRegistry.ts`
- ACP catalog QA: `packages/tests/suites/ui-e2e/settings.acpCatalog.spec.ts`

| Provider | Current shape | Normal sessions | Execution runs | Direct sessions / imports | Transcript sources | Controls / permissions | Installables / auth | Tests / manual QA | Recommendation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| PI | `bespoke ACP` with a native RPC backend.<br>`packages/plugins/pi/src/agent/**` | Shipped via `runStandardAcpProvider`, but runtime is PI-owned.<br>`packages/plugins/pi/src/agent/**` | Shipped.<br>`packages/plugins/pi/src/agent/**`<br>registered in `apps/cli/src/agent/executionRuns/registry/executionRunBackendRegistry.ts` | No direct-session browser/import surface.<br>No `directSessions/` or `handoff/` tree.<br>But backend-level `loadSession()` exists and restarts with `--session`.<br>`packages/plugins/pi/src/agent/**` | Happier transcript is primary; PI session identity is published from runtime metadata.<br>`packages/plugins/pi/src/agent/**`<br>RPC event mapping is PI-specific.<br>`packages/plugins/pi/src/agent/**` | Tool set is derived from Happier permission mode and PI thinking level.<br>`packages/plugins/pi/src/agent/**`<br>`packages/agents/src/providers/pi/thinking.ts`<br>PI does not attach MCP servers in normal runs.<br>`packages/plugins/pi/src/agent/**` | Managed package install path exists.<br>`packages/agents/src/cli/runtime.ts`<br>Auth is env-only today (`OPENAI_API_KEY` / `ANTHROPIC_API_KEY`).<br>`packages/plugins/pi/src/agent/**`<br>`apps/docs/content/docs/providers/pi.mdx` | Strong unit coverage on RPC/load-session path.<br>`packages/plugins/pi/src/agent/**`<br>Provider harness spec exists.<br>`packages/plugins/pi/src/agent/**` | Keep native / bespoke ACP.<br>Do not force into plain catalog+hook until PI RPC, thinking, and deterministic `--session` resume can be expressed as first-class hooks. |
| Qwen | Thin `bespoke ACP` wrapper around generic `AcpBackend`.<br>`packages/plugins/qwen/src/agent/**` | Shipped through the standard ACP provider loop.<br>`packages/plugins/qwen/src/agent/**` | Shipped.<br>`packages/plugins/qwen/src/agent/**` | No direct-session browser/import flow.<br>`packages/agents/src/manifest.ts` marks `sessionStorage.direct: false`.<br>Any `session/load` support is only ACP-capability-driven, not a user-facing direct-sessions surface.<br>`packages/plugins/qwen/src/agent/**`<br>`apps/cli/src/capabilities/probes/createAcpCliCapability.ts` | Happier transcript plus vendor session-id metadata only.<br>`packages/plugins/qwen/src/agent/**` | Qwen-specific permission args are isolated already.<br>`packages/plugins/qwen/src/agent/**`<br>Transport is small and ACP-shaped.<br>`packages/plugins/qwen/src/agent/**` | Managed package install path exists.<br>`packages/agents/src/cli/runtime.ts`<br>CLI auth-status detection is still `unknown`, but Happier now has a native `/auth` login-launch flow; the remaining gap is auth probing, not launch support.<br>`packages/plugins/qwen/src/agent/**`<br>`apps/docs/content/docs/providers/qwen.mdx` | Backend/runtime tests exist and provider harness spec exists.<br>`packages/plugins/qwen/src/agent/**` | Move toward `catalog+hook`.<br>Needed hooks: spawn-arg hook for `--acp` + approval mapping, optional auth-status probing hook, optional transport hook. |
| Kilo | Thin `bespoke ACP` wrapper; structurally very close to catalog+hook already.<br>`packages/plugins/kilo/src/agent/**` | Shipped through standard ACP provider flow.<br>`packages/plugins/kilo/src/agent/**` | Shipped.<br>`packages/plugins/kilo/src/agent/**` | No direct-session browser/import UX.<br>`packages/agents/src/manifest.ts` keeps `direct: false`.<br>ACP load-session probing is explicitly tested, so resume/import capability can be discovered at the CLI layer.<br>`packages/plugins/kilo/src/agent/**` | Happier transcript plus vendor session-id metadata.<br>`packages/plugins/kilo/src/agent/**` | Main special case is OpenCode-family permission env bridging, not a custom runtime.<br>`packages/plugins/kilo/src/agent/**`<br>`packages/plugins/opencode/src/agent/**` | Managed package install path exists.<br>`packages/agents/src/cli/runtime.ts`<br>CLI auth-status detection is still `unknown`, but Happier now supports the `/connect` login-launch flow; the remaining gap is auth probing.<br>`packages/plugins/kilo/src/agent/**`<br>`apps/docs/content/docs/providers/kilo.mdx` | Good unit coverage plus provider harness spec.<br>`packages/plugins/kilo/src/agent/**` | Move to `catalog+hook`.<br>Needed hooks: permission-env hook, transport hook, optional auth-status probing hook, optional dynamic load-session capability exposure. |
| Copilot | Thin `bespoke ACP` wrapper.<br>`packages/plugins/copilot/src/agent/**` | Shipped through standard ACP provider flow.<br>`packages/plugins/copilot/src/agent/**` | Shipped.<br>`packages/plugins/copilot/src/agent/**` | No direct-session browser/import flow.<br>`packages/agents/src/manifest.ts` keeps `direct: false`.<br>No provider e2e spec for direct/import behavior. | Happier transcript plus vendor session-id metadata.<br>`packages/plugins/copilot/src/agent/**` | Main special case is permission mapping to `--yolo` instead of env policy.<br>`packages/plugins/copilot/src/agent/**`<br>Transport is still conventional ACP.<br>`packages/plugins/copilot/src/agent/**` | Managed package install path exists.<br>`packages/agents/src/cli/runtime.ts`<br>Auth is better than most ACP wrappers: env tokens plus `gh auth token` fallback.<br>`packages/plugins/copilot/src/agent/**`<br>`apps/docs/content/docs/providers/copilot.mdx` | Some unit coverage exists, but there is no provider harness spec today.<br>Missing `packages/plugins/copilot/src/agent/e2e/providerSpec.json` is the biggest QA gap. | Move to `catalog+hook`.<br>Needed hooks: spawn-arg hook for `--acp/--yolo`, auth-status hook using `gh`, transport hook.<br>Add provider harness coverage before large migration. |
| Auggie | Thin `bespoke ACP` wrapper with one extra runtime flag.<br>`packages/plugins/auggie/src/agent/**` | Shipped through standard ACP provider flow.<br>`packages/plugins/auggie/src/agent/**` | Shipped.<br>`packages/plugins/auggie/src/agent/**` | No direct-session browser/import flow.<br>`packages/agents/src/manifest.ts` keeps `direct: false`. | Happier transcript plus vendor session-id metadata.<br>`packages/plugins/auggie/src/agent/**` | Special cases are `--allow-indexing` and provider-specific permission args.<br>`packages/plugins/auggie/src/agent/**` | Managed package install path exists.<br>`packages/agents/src/cli/runtime.ts`<br>CLI auth-status detection is still `unknown`, but Happier now supports `auggie login`; the remaining gap is auth probing.<br>`packages/plugins/auggie/src/agent/**`<br>`apps/docs/content/docs/providers/augment.mdx` | Backend/runtime tests exist and provider harness spec exists.<br>`packages/plugins/auggie/src/agent/**` | Move to `catalog+hook`.<br>Needed hooks: extra-arg hook for indexing, permission-arg hook, transport hook, optional auth-status probing hook. |
| Kimi | Thin-to-medium `bespoke ACP` wrapper; still much thinner than Gemini/PI.<br>`packages/plugins/kimi/src/agent/**` | Shipped through standard ACP provider flow.<br>`packages/plugins/kimi/src/agent/**` | Shipped.<br>`packages/plugins/kimi/src/agent/**` | No direct-session browser/import flow.<br>`packages/agents/src/manifest.ts` keeps `direct: false`. | Happier transcript plus vendor session-id metadata.<br>`packages/plugins/kimi/src/agent/**` | Two real special cases: read-only mode uses a generated agent YAML, and Kimi ACP rejects MCP servers so Happier disables MCP attachment.<br>Kimi model selection now uses shared dynamic ACP probing and accepts Kimi's `modelId` payload shape.<br>`packages/plugins/kimi/src/agent/**`<br>`apps/cli/src/capabilities/probes/agentModelsProbe.ts`<br>`packages/agents/src/models.ts` | Vendor recipe install path exists.<br>`packages/agents/src/cli/runtime.ts`<br>CLI auth-status detection is still `unknown`, but Happier now supports the `/setup` login-launch flow; the remaining gap is auth probing.<br>`packages/plugins/kimi/src/agent/**`<br>`apps/docs/content/docs/providers/kimi.mdx` | Good unit coverage plus provider harness spec.<br>`packages/plugins/kimi/src/agent/**`<br>`apps/cli/src/capabilities/probes/agentModelsProbe.staticOnly.test.ts`<br>`apps/cli/src/capabilities/probes/agentModelsProbe.integration.test.ts`<br>`packages/plugins/kimi/src/agent/**` | Move to `catalog+hook` only if hook model can express: generated read-only agent file, `--yolo`, and `mcpServers: undefined` override. Remaining migration hooks are spawn args, transport, permission mapping, and auth-status probing. |
| Kiro | Already `catalog+hook` in spirit and mostly in code.<br>`packages/agents/src/acp.ts`<br>`apps/cli/src/agent/acp/catalog/createCatalogDefinedAcpEntry.ts`<br>`apps/cli/src/agent/acp/catalog/createCatalogDefinedAcpBackend.ts`<br>`apps/cli/src/agent/acp/catalog/runCatalogDefinedAcpAgent.ts`<br>`packages/plugins/kiro/src/agent/**` | Shipped through the built-in generic ACP path, not a provider-owned run file.<br>`packages/agents/src/acp.ts` sets `transportProfile: 'kiro'` and `supportsLoadSession: true`. | `missing` as a dedicated execution-run backend.<br>No provider-specific execution-run leaf under `packages/plugins/kiro/src/agent/executionRuns/**`.<br>No registry entry in `apps/cli/src/agent/executionRuns/registry/executionRunBackendRegistry.ts`. | No direct-session browser/import surface despite `sessionStorage.direct: true` in `packages/agents/src/manifest.ts` and built-in ACP `supportsLoadSession: true` in `packages/agents/src/acp.ts`.<br>That means attach/load exists conceptually, but UI direct-sessions work has not been built. | Transcript source stays generic ACP/Happier transcript; no provider-specific session-id metadata helper exists in the Kiro tree.<br>Transport special casing is only stderr filtering.<br>`packages/plugins/kiro/src/agent/**` | Main control/auth special cases are generic catalog auth + Kiro `whoami --format json` probing.<br>`packages/agents/src/auth.ts`<br>`apps/cli/src/agent/acp/catalog/auth/createCatalogDefinedCliAuthSpec.ts` | Manual install only; no managed install path.<br>`packages/agents/src/cli/runtime.ts`<br>Docs are aligned with built-in ACP positioning.<br>`apps/docs/content/docs/providers/kiro.mdx` | Very light coverage only.<br>`packages/plugins/kiro/src/agent/**`<br>No provider harness spec, no execution-run tests. | Keep `catalog+hook`.<br>Do not re-native it.<br>Next work should add missing execution-run registration and decide whether direct-session UX should really exist for Kiro. |
| Gemini | `native` / heavy bespoke ACP runtime; this is the contrast case.<br>`packages/plugins/gemini/src/agent/**` | Shipped, but normal session flow is provider-owned rather than `runStandardAcpProvider`.<br>`packages/plugins/gemini/src/agent/**` | Shipped.<br>`packages/plugins/gemini/src/agent/**` | No direct-session browser/import flow.<br>`packages/agents/src/manifest.ts` keeps `direct: false`. | Happier transcript plus Gemini-specific streaming/transcript machinery.<br>`packages/plugins/gemini/src/agent/**` | Gemini owns model resolution, auth-method selection, sandbox toggle, MCP environment prep/cleanup, and runtime message handling.<br>`packages/plugins/gemini/src/agent/**` | Managed package install path exists.<br>`packages/agents/src/cli/runtime.ts`<br>Rich auth probe and connected-services/cloud-connect support exist.<br>`packages/plugins/gemini/src/agent/**`<br>`apps/docs/content/docs/providers/gemini.mdx` | Deep unit coverage plus provider harness spec.<br>`packages/plugins/gemini/src/agent/**` | Keep native / bespoke ACP.<br>Gemini is the clear example of a provider that should not be collapsed into plain catalog+hook soon. |

## Migration notes by bucket

### Keep native / bespoke

- PI: keep provider-owned until generic hooks can express RPC transport, PI thinking, explicit tool menus, and deterministic `--session` restarts.
- Gemini: keep provider-owned because model/auth/MCP/runtime behavior is substantially richer than the thin ACP wrappers.

### Best catalog-plus-hook candidates

- Qwen
- Kilo
- Copilot
- Auggie
- Kimi

Shared migration target for that group:

- Use the generic catalog runtime path from `apps/cli/src/agent/acp/runtime/createCatalogProviderAcpRuntime.ts`.
- Replace provider-owned `index.ts` / `run*.ts` glue with a catalog entry plus small hooks for spawn args, transport, auth-launch, and permission mapping.
- Keep provider-specific session-id metadata helpers until metadata publication is unified.
- Preserve execution-run registration in `apps/cli/src/agent/executionRuns/registry/executionRunBackendRegistry.ts` while moving factories behind catalog descriptors.

### Already effectively catalog-defined

- Kiro: treat it as the reference implementation for built-in catalog ACP. The gap is not architecture, it is missing surfaces: execution-run registration, stronger tests, and a deliberate decision on whether direct-session UX should exist.

## Biggest current gaps

- Copilot and Kiro have no provider harness spec file under `packages/plugins/<providerId>/src/agent/e2e/providerSpec.json`.
- Kiro has no execution-run registration.
- Most catalogizable ACP providers still ship `createUnknownCliAuthSpec(...)` for auth-status detection, even though native login-launch flows are already implemented for Qwen, Kilo, Kimi, and Auggie.
- None of PI, Qwen, Kilo, Copilot, Auggie, Kimi, or Gemini currently ship a direct-session browser/import surface comparable to Codex / Claude / OpenCode.
