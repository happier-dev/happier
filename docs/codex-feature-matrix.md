# Archived: Codex Feature Matrix

> **Historical snapshot — not current implementation guidance.** This matrix predates the completed Agent/Provider vocabulary split and plugin-owned UI migration. Its `provider` terminology, removed `packages/agents/src/providerSettings/**` paths, and retired `apps/ui/sources/agents/providers/**` paths are preserved only to explain older migration decisions. Use [Agents catalog](./agents-catalog.md) for executable Agent ownership and [Providers](./providers.md) for model-source architecture.

This file must not be used to locate current owners or plan new changes. Re-derive any historical claim from the current plugin contribution, Agent catalog, and Provider contracts before acting on it.

## Status legend

- `supported`: primary or fully wired path
- `partial`: works, but only on some transports or with notable gaps
- `legacy`: still wired, but no longer the preferred path
- `unsupported`: intentionally absent today

## Runtime surfaces

| Feature | Status | Current owner / source areas | Special cases | Unified architecture migration notes |
| --- | --- | --- | --- | --- |
| Backend mode selection (`appServer` / `acp` / `mcp`) | `supported` | `packages/agents/src/providerSettings/definitions/codex.ts`, `apps/ui/sources/agents/providers/codex/settings/plugin.ts`, `apps/ui/sources/agents/providers/codex/uiBehavior.ts`, `packages/plugins/codex/src/agent/**`, `apps/cli/src/rpc/handlers/spawnSessionOptionsContract.ts` | Account default is `appServer`; legacy `mcp_resume` is normalized to `acp`; resume/wake payloads persist `codexBackendMode` separately from the old `experimentalCodexAcp` flag. | Remove the ACP-era boolean shadow flag and carry one transport-neutral runtime descriptor from settings -> spawn payload -> persisted session metadata. |
| App-server remote runtime | `supported` | `packages/plugins/codex/src/agent/**` | This is the default remote path; it publishes generic `sessionModesV1` / `sessionModelsV1` / `sessionConfigOptionsV1` metadata, supports in-flight steer, native fork, and latest-turn rollback. | Treat this as the canonical Codex session runtime and make the transport an internal detail rather than a mode the rest of the stack has to reconstruct. |
| ACP remote runtime | `supported` | `packages/plugins/codex/src/agent/**` | ACP still carries Codex resume, runtime-installable, and execution-run coverage; it supports in-flight steering but explicitly reports rollback unsupported. | Keep ACP as a compatibility transport, but stop exposing ACP-specific semantics outside the backend boundary. |
| Plain MCP remote runtime | `legacy` | `packages/plugins/codex/src/agent/**` | MCP remains the fallback when ACP cannot start and for some execution-run cases; vendor resume is fail-closed on plain MCP. | Either fully hide MCP behind the same provider-session contract or keep it explicitly as a degraded fallback with fewer capabilities. |
| Normal session startup / daemon spawn | `supported` | `packages/plugins/codex/src/agent/**` | Daemon spawn hooks seed temporary `CODEX_HOME` for token auth, validate ACP only when ACP is the selected mode, and still publish the ACP env marker for child processes. | Collapse daemon auth env, transport choice, and provider settings into one launch contract instead of recomputing them in spawn hooks and runtime entrypoints. |
| Execution runs | `supported` | `packages/plugins/codex/src/agent/**`, `apps/cli/src/agent/executionRuns/registry/executionRunBackendRegistry.ts` | Execution runs now include an app-server backend and prefer app-server by default when probing succeeds; they still fall back to ACP or MCP when needed and preserve explicit transport overrides. | Remaining work is capability parity and cleanup of the fallback selection logic so execution runs feel less transport-divergent from normal Codex sessions. |

## Session lifecycle surfaces

| Feature | Status | Current owner / source areas | Special cases | Unified architecture migration notes |
| --- | --- | --- | --- | --- |
| Direct session browse/list | `supported` | `packages/plugins/codex/src/agent/**`, `apps/ui/sources/agents/providers/codex/directSessions/resolveCodexBrowseSourceOptions.ts`, `apps/ui/sources/agents/providers/codex/uiBehavior.ts` | UI exposes user-home and `openai-codex` connected-service homes; listing prefers app-server `thread/list` and falls back to rollout file discovery; candidates are branded `codexBackendMode: 'appServer'`. | Promote direct-session source and transport affinity into one provider session source record shared by browse, wake, takeover, and handoff. |
| Transcript source: persisted Happier transcript | `supported` | `packages/plugins/codex/src/agent/**` | App-server streaming writes assistant/thinking deltas into Happier; ACP also publishes vendor session identity into Happy metadata; persisted transcript is the default unless `HAPPIER_TRANSCRIPT_STORAGE=direct`. | Keep one transcript-ingestion pipeline and avoid transport-specific metadata branching for persisted sessions. |
| Transcript source: linked direct transcript | `supported` | `packages/plugins/codex/src/agent/**` | `directSessionV1` is only published when transcript storage is `direct`; direct paging is backward-only and tailing uses a provider-specific forward cursor with an app-server preview fallback when rollout files are absent. | Replace `directSessionV1` and Codex-only cursor shapes with a single transcript-source envelope and generic direct-history cursor contract. |
| Direct import / takeover into a Happy session | `supported` | `apps/cli/src/api/directSessions/takeover/resolveDirectTakeoverSpawnOptions.ts`, `apps/cli/src/api/machine/rpcHandlers.directSessions.ts`, `packages/plugins/codex/src/agent/**` | Takeover re-spawns with `resume`, `transcriptStorage: 'direct'`, `CODEX_HOME`, and any linked `codexBackendMode`; current direct-session candidates effectively route this through app-server semantics. | Reframe takeover as a provider-session source transition instead of a direct-session special case layered on top of spawn options. |
| Local/remote switching | `supported` | `packages/plugins/codex/src/agent/**` | Local control is only enabled for `acp` and `appServer`, not plain MCP; switch-to-local discards queued/pending messages first and mirrors rollout-derived transcript state back into Happier. | Move transport-aware local-control gating into capability metadata so switching logic no longer depends on backend-mode conditionals in `runCodex.ts`. |
| Dynamic controls: collaboration modes / models / config options | `supported` | `packages/plugins/codex/src/agent/**`, `apps/ui/sources/agents/providers/codex/core.ts`, `apps/ui/sources/agents/providers/codex/uiBehavior.ts` | App-server reads `collaborationMode/list` and `model/list`, publishes generic session-control metadata, and currently supports one config option (`speed`) gated to `gpt-5.4` plus compatible auth methods. | Keep the generic state keys and extend the same controls contract to ACP/MCP if they ever gain parity; avoid reintroducing ACP-branded metadata for Codex. |
| In-flight steering | `partial` | `packages/plugins/codex/src/agent/**` | App-server and ACP advertise `inFlightSteer`; app-server implements `turn/steer`, ACP uses the ACP steer entrypoint, and plain MCP has no equivalent. | Surface steering as one capability on the provider session contract and let the runtime choose the correct transport implementation. |
| Fork conversation | `partial` | `packages/plugins/codex/src/agent/**`, `apps/cli/src/api/machine/rpcHandlers.ts`, `apps/ui/sources/agents/providers/codex/uiBehavior.ts` | UI currently reports `supportsForkConversation` only for `appServer` and `supportsForkFromMessage` as false; app-server native fork exists, but message-point fork is not exposed for Codex. | Keep app-server native fork, but normalize it behind the shared provider-native fork dispatch so Codex stops looking like a one-off fork implementation. |
| Rollback latest turn | `supported` | `packages/plugins/codex/src/agent/**`, `apps/ui/sources/agents/providers/codex/uiBehavior.ts`, `apps/ui/sources/sync/domains/sessionRollback/rollbackUiSupport.ts` | Only app-server sessions expose rollback; runtime calls `thread/rollback`, rejects while a turn is in flight, and persists `sessionRollbackRangesV1` from completed-turn seq ranges. ACP explicitly returns `unsupported_action`; MCP has no rollback path. | Keep rollback under the shared session-action capability map and do not duplicate transport-specific UI gating beyond the provider capability check. |
| Manual resume (`happier resume`, explicit `--resume`) | `supported` | `packages/plugins/codex/src/agent/**`, `apps/ui/sources/agents/providers/codex/uiBehavior.ts`, `packages/agents/src/session/controls/vendorResumePolicy.ts`, `apps/cli/src/cli/commands/resume.ts` | Resume is allowed only for `appServer` and `acp`; `runCodex.ts` fails closed on plain MCP, seeds permission/model overrides from the startup cache, and persists `codexSessionId` plus `codexBackendMode`. | Replace the current resume heuristics, startup cache, and backend-mode reconstruction with one serialized provider resume descriptor. |
| Wake / background resume | `supported` | `apps/ui/sources/agents/providers/codex/uiBehavior.ts`, `apps/ui/sources/agents/runtime/resumeCapabilities.ts`, `packages/agents/src/session/controls/vendorResumePolicy.ts`, `apps/cli/src/daemon/startDaemon.ts` | Wake payloads prefer persisted `codexBackendMode` from session metadata, then fall back to account settings; this is still coupled to Codex-specific extras building. | Serialize wake-ready runtime state once at session creation and reuse it directly instead of rebuilding Codex spawn extras at wake time. |
| Manual cross-machine handoff | `partial` | `packages/plugins/codex/src/agent/**`, `packages/agents/src/manifest.ts`, `apps/cli/src/session/handoff/types.ts` | Export ships rollout files from the linked `CODEX_HOME`/source affinity when available; import preserves `codexBackendMode` plus the canonical runtime descriptor and still defaults transcript storage to `direct` unless persisted mode is requested. | Direct-session takeover and handoff import are much closer now because both preserve backend/runtime affinity; the remaining gap is proving this cleanly on a healthy runtime stack. |

## Controls, permissions, and auth surfaces

| Feature | Status | Current owner / source areas | Special cases | Unified architecture migration notes |
| --- | --- | --- | --- | --- |
| Permission policy mapping | `supported` | `packages/plugins/codex/src/agent/**` | One central mapping feeds MCP sandbox/approval policy and app-server sandbox/approval policy; permission mode updates can arrive from message metadata mid-session. | Keep the single policy map, but move permission publication and transport application behind a provider capability service instead of threading handlers through `runCodex.ts`. |
| Installables / managed runtime dependency | `supported` | `packages/plugins/codex/src/agent/**`, `apps/ui/sources/agents/providers/codex/uiBehavior.ts` | Only Codex ACP is a runtime installable; app-server and MCP are system-provided. UI only flags the installable as relevant when ACP mode is selected. | Make installable requirements transport-aware in one provider runtime policy so ACP-specific checks do not leak through UI behavior and daemon hooks separately. |
| CLI auth detection | `supported` | `packages/plugins/codex/src/agent/**` | Auth detection checks `codex login status`, falls back to `OPENAI_API_KEY`, and finally to `~/.codex/auth.json`; probe timeout is configurable. | Move auth probing behind one transport-neutral provider auth contract and stop coupling UI state to Codex CLI command parsing. |
| Connected-services auth materialization / cloud auth | `supported` | `packages/plugins/codex/src/agent/**`, `packages/agents/src/manifest.ts` | Supported services are `openai-codex` OAuth and `openai` token; materialization writes `auth.json` into a generated `CODEX_HOME`, and cloud auth exposes device / paste / API-key flows. | Normalize connected-service materialization and interactive cloud auth into explicit provider auth adapters with declared env/filesystem outputs. |

## Tests and QA surfaces

| Feature | Status | Current owner / source areas | Special cases | Unified architecture migration notes |
| --- | --- | --- | --- | --- |
| CLI/unit/integration coverage | `supported` | `packages/plugins/codex/src/agent/**` | Coverage is broad across app-server runtime, ACP runtime, direct-session listing, installables, auth, and local-control state transitions. | Preserve the transport-specific tests, but organize future work around shared provider capability contracts rather than ad hoc mode-by-mode fixtures. |
| Provider contract / scenario coverage | `supported` | `packages/plugins/codex/src/agent/**`, `packages/tests/src/testkit/providers/scenarios/scenarios.codex.ts`, `packages/tests/suites/agents/scenarioCatalog.codexResumeInactivity.test.ts` | Provider harness coverage still skews toward ACP/load-session semantics because that is the long-lived contract harness. | Add explicit app-server scenarios into the same provider contract layer so Codex does not look like two loosely related providers. |
| Core e2e runtime coverage | `supported` | `packages/tests/suites/core-e2e/codex.appServer.latestTurnRollback.slow.e2e.test.ts`, `packages/tests/suites/core-e2e/codex.acp.inFlightSteer.pendingQueue.slow.e2e.test.ts`, `packages/tests/suites/core-e2e/codex.mcp.noRestart.permissionChange.slow.e2e.test.ts`, `packages/tests/suites/core-e2e/codex.switch.remoteToLocal.mirroring.slow.e2e.test.ts`, `packages/tests/suites/core-e2e/codex.switch.remoteToLocal.failClosed.pending.slow.e2e.test.ts`, `packages/tests/suites/core-e2e/directSessions.codex.appServer.browseTail.feat.sessions.direct.e2e.test.ts`, `packages/tests/suites/core-e2e/directSessions.codex.appServer.takeoverPersistContinue.feat.sessions.direct.slow.e2e.test.ts`, `packages/tests/suites/core-e2e/directSessions.codex.browseTail.feat.sessions.direct.e2e.test.ts`, `packages/tests/suites/core-e2e/connectedServices.codex.materialize.feat.connectedServices.slow.e2e.test.ts`, `packages/tests/suites/core-e2e/daemon.spawn.codex.tempHome.seedsConfig.slow.e2e.test.ts` | Codex is one of the few providers with explicit coverage across app-server rollback, ACP in-flight steer, MCP permission-policy behavior, direct-session browse/takeover, and local/remote switching. | Keep this breadth, but align test naming and fixtures around capability families rather than backend-mode names where possible. |
| UI e2e and manual QA surfaces | `supported` | `packages/tests/suites/ui-e2e/session.codex.appServer.dynamicControls.spec.ts`, `packages/tests/suites/ui-e2e/session.codex.appServer.forkRollback.spec.ts`, `packages/tests/suites/ui-e2e/session.codex.appServer.forkFromInfo.spec.ts`, `packages/tests/suites/ui-e2e/session.direct.codex.browse.openTail.feat.sessions.direct.spec.ts`, `packages/tests/suites/ui-e2e/settings.providers.authTerminal.spec.ts`, `apps/docs/content/docs/providers/codex.mdx`, `apps/docs/content/docs/features/direct-sessions.mdx`, `apps/docs/content/docs/features/attach-to-session.mdx`, `apps/docs/content/docs/features/session-forking.mdx`, `apps/docs/content/docs/features/session-resume.mdx`, `apps/docs/content/docs/features/installables.mdx` | There is no separate Codex-only manual QA tracker in `docs/testing/`; the practical QA surface is the UI e2e suite plus provider/feature docs that encode expected workflows. | Add one internal Codex QA checklist only if it stays transport-aware and replaces doc-fragment scavenging rather than becoming another divergent source of truth. |

## Cross-cutting migration themes

1. Codex still has three live runtime paths with materially different capability envelopes: app-server (richest), ACP (compatibility + installable), and MCP (fallback). The architecture should preserve the transports but stop making the rest of the product reason about them directly.
2. Resume, wake, direct sessions, and handoff all reconstruct the same identity tuple in different ways: `codexSessionId`, `codexBackendMode`, `CODEX_HOME`, transcript storage, working directory, and runtime descriptor/provider-extra payloads. That tuple is now substantially serialized, but some compatibility fields still remain and should continue collapsing into the canonical provider runtime descriptor.
3. App-server already publishes transport-neutral session-control metadata (`sessionModesV1`, `sessionModelsV1`, `sessionConfigOptionsV1`), and handoff import now preserves canonical Codex runtime affinity without `experimentalCodexAcp`; some spawn/resume paths still carry ACP-era compatibility fields.
4. Execution runs are no longer ACP/MCP-only; they now have an app-server path and prefer it by default when available. The remaining work is parity polish and cleanup of the fallback seams.
5. Direct-session takeover and handoff imports are now much closer: both preserve backend/runtime affinity. The remaining stability question is healthy-stack proof rather than missing source wiring.
