# Archived: OpenCode Feature Matrix

> **Historical snapshot — not current implementation guidance.** This matrix predates the completed Agent/Provider vocabulary split and plugin-owned UI migration. Its `provider` terminology and retired `apps/ui/sources/agents/providers/**` paths are preserved only to explain older migration decisions. Use [Agents catalog](./agents-catalog.md) for executable Agent ownership and [Providers](./providers.md) for model-source architecture.

This file must not be used to locate current owners or plan new changes. Re-derive any historical claim from the current plugin contribution, Agent catalog, and Provider contracts before acting on it.

## Status legend

- `supported`: primary current path
- `partial`: works, but only on one runtime path or with notable gaps
- `legacy`: still wired, but no longer the preferred path
- `unsupported`: intentionally absent today

## Runtime surfaces

| Feature | Status | Current owner / source areas | Special cases | Unified architecture migration notes |
| --- | --- | --- | --- | --- |
| Server mode (server-native OpenCode sessions) | `supported` | `packages/plugins/opencode/src/agent/**` | Default path unless `HAPPIER_OPENCODE_BACKEND_MODE=acp`; can use shared managed loopback server or explicit pinned server URL; metadata persists `opencodeBackendMode`, `opencodeSessionId`, and explicit server affinity. | Make this the single canonical runtime surface; keep server affinity in one provider-runtime record instead of spreading it across env vars plus session metadata fields. |
| ACP mode | `legacy` | `packages/plugins/opencode/src/agent/**` | Still fully wired for explicit ACP sessions and powers some compatibility paths, but docs position it as fallback/legacy. | Collapse ACP-specific transport/runtime shims behind the same provider session contract as server mode, or retire ACP-only metadata once server-native parity exists. |
| Normal session startup / daemon spawn | `supported` | `packages/plugins/opencode/src/agent/**` | `--happy-starting-mode` can seed local vs remote only for server mode; daemon spawn validation is generic provider CLI validation. | Replace backend-mode-specific startup branching with one runtime descriptor that chooses transport/control strategy internally. |
| Execution runs | `supported` | `packages/plugins/opencode/src/agent/**`, `apps/cli/src/agent/executionRuns/registry/executionRunBackendRegistry.ts`, `apps/cli/src/capabilities/registry/toolExecutionRuns.ts` | Execution runs now default to the server-native backend and only use ACP when backend mode explicitly selects ACP. UI/runtime metadata still carry some compatibility seams, but execution-run transport is no longer ACP-only. | Remaining work is cleanup so the server-native/runtime-descriptor path becomes the sole source of truth instead of coexisting with ACP-era fallback assumptions. |

## Session lifecycle surfaces

| Feature | Status | Current owner / source areas | Special cases | Unified architecture migration notes |
| --- | --- | --- | --- | --- |
| Direct session browse/list | `supported` | `packages/plugins/opencode/src/agent/**`, `apps/ui/sources/agents/providers/opencode/directSessions/resolveOpenCodeBrowseSourceOptions.ts`, `apps/ui/sources/agents/providers/opencode/uiBehavior.ts` | Only `opencodeServer` direct sources are exposed; list activity is derived from server status plus timestamps. | Promote direct-session source descriptors into the same provider session record used by normal sessions so browse, resume, and takeover all share one source model. |
| Transcript source: persisted Happier transcript | `supported` | `packages/plugins/opencode/src/agent/**` | Server-native runtime streams drafts/commits into Happier and imports resume history best-effort. | Keep one normalized transcript ingestion pipeline regardless of provider transport so server/native/direct imports do not fork message metadata conventions. |
| Transcript source: linked direct transcript | `supported` | `packages/plugins/opencode/src/agent/**` | `directSessionV1` is only written when transcript storage is `direct` and backend mode is `server`; direct paging is backward/tail based and does not support `direction='newer'` in the history pager. | Replace `directSessionV1` plus provider-specific source payloads with a single transcript-source envelope that can describe persisted, direct-live, and imported histories uniformly. |
| Direct takeover -> persisted session | `supported` | `apps/cli/src/api/session/external/takeover/resolveExternalTakeoverSpawnOptions.ts`, `apps/cli/src/api/machine/rpcHandlers.directSessions.ts` | Takeover imports direct transcript first, clears `directSessionV1`, writes `externalHistoryImportV1`, then respawns as `persisted`; OpenCode takeover forces server mode env vars. | Treat takeover as a source transition on one session record instead of a special import + respawn path with metadata cleanup. |
| Local/shared attach | `supported` | `packages/plugins/opencode/src/agent/**` | Available only for server mode with a TTY; topology is shared, remote stays writable while attached local TUI is active; attach uses provider-native `opencode attach ...`. | Model attach/detach as one generic local-control capability with provider-specific attach executors, so shared/exclusive differences live in capability metadata rather than ad hoc runtime branches. |
| Dynamic controls: session modes/models | `partial` | `packages/plugins/opencode/src/agent/**`, `apps/cli/src/capabilities/probes/agentModelsProbe.ts`, `apps/ui/sources/agents/providers/opencode/uiBehavior.ts` | Server mode now publishes `sessionModesV1` alongside legacy `acpSessionModesV1`; models still remain under `acpSessionModelsV1`. Preflight model probing prefers `opencode models`; current controls are still metadata-driven rather than fully unified runtime control objects. | Continue converging ACP-shaped metadata onto one transport-neutral session-controls contract. |
| Dynamic controls: permission intent mapping | `supported` | `packages/plugins/opencode/src/agent/**` | Server mode computes permission rulesets per session; ACP mode applies permission env at process start; effective behavior still depends on installed OpenCode runtime. | Unify permission publishing so both runtime paths consume the same resolved permission contract and expose the same observable metadata. |
| Fork: latest/message-native fork | `supported` | `packages/plugins/opencode/src/agent/**`, `apps/cli/src/api/machine/rpcHandlers.ts`, `apps/ui/sources/agents/providers/opencode/uiBehavior.ts` | Native fork-at-message is only available for server mode; ACP path only supports latest-session fork when ACP backend exposes `forkSession`; replay remains the fallback. | Keep provider-native fork as the primary path, but move fork-point resolution and metadata patching into a provider-agnostic session-fork service. |
| Rollback latest turn | `unsupported` | `apps/ui/sources/agents/providers/opencode/uiBehavior.ts`, `apps/ui/sources/sync/domains/sessionRollback/rollbackUiSupport.ts` | OpenCode UI behavior wires forking but does not advertise rollback support, and there is no OpenCode `session.rollback` runtime hook. | If OpenCode gains native rollback, add it through the same session action capability map used by Codex instead of introducing a separate OpenCode-only RPC shape. |
| Revert semantics | `unsupported` | `apps/cli/src/api/machine/rpcHandlers.ts`, `apps/ui/sources/agents/providers/opencode/uiBehavior.ts` | There is no OpenCode provider-session revert primitive today; only fork or replay-style recovery exists. | Treat revert as either a future provider action capability or keep it explicitly out of the unified session-action matrix. |
| Manual resume (`happier resume`, explicit `--resume`) | `supported` | `packages/agents/src/manifest.ts`, `packages/plugins/opencode/src/agent/**`, `apps/cli/src/cli/commands/resume.ts` | Resume depends on `opencodeSessionId` metadata being published; server runtime imports history on fresh-session resume and suppresses duplicate imports when metadata markers already exist. | Keep a single vendor-resume record with source affinity, history-import marker, and runtime mode instead of scattered metadata keys plus resume-side heuristics. |
| Wake resume / background wake | `supported` | `apps/ui/sources/agents/providers/opencode/uiBehavior.ts`, `apps/ui/sources/agents/runtime/resumeCapabilities.ts` | Wake payload rebuilds env from session metadata first, then falls back to account settings for backend mode and explicit server URL. | Replace env reconstruction with a serialized provider runtime descriptor that the daemon can wake directly. |
| Manual cross-machine handoff | `supported` | `packages/plugins/opencode/src/agent/**` | Handoff preserves server affinity and returns both a direct source and a resume plan; imported sessions default to `direct` transcript storage unless persisted mode is requested. | Merge handoff bundle affinity, direct-source metadata, and resume metadata into one canonical provider-session export/import payload. |

## Installables, auth, and coverage

| Feature | Status | Current owner / source areas | Special cases | Unified architecture migration notes |
| --- | --- | --- | --- | --- |
| Provider installables | `supported` | `packages/plugins/opencode/src/manifest.ts`, `packages/cli-common/src/agents/install.ts`, `packages/cli-common/src/agents/resolution.ts`, `apps/cli/src/capabilities/rpc/handlers/capabilities.ts`, `apps/docs/content/docs/providers/opencode.mdx` | The OpenCode plugin declares a managed `opencode-ai` installation while generic agent resolution remains system-first and preserves the shared `tools/providers/opencode` layout. Installation is explicit through the capability Install action, the legacy-named `happier install provider opencode` command, or stack provisioning via `HAPPIER_PROVIDER_CLIS`; daemon startup and session spawn do not install agents implicitly. | Keep provider facts in the OpenCode plugin and installation/resolution in the generic agent owners. Provider spawn should continue to consume that result and fail closed when OpenCode is not provisioned. |
| CLI auth detection | `supported` | `packages/plugins/opencode/src/agent/**` | Auth status is derived from `opencode auth list`; probe timeout is configurable; failure collapses to logged-out or unknown. | Move provider auth probing behind one transport-neutral auth-status contract so UI and CLI stop depending on provider-specific command parsing details. |
| Connected-services auth materialization | `supported` | `packages/plugins/opencode/src/agent/auth/services/**`, `apps/cli/src/daemon/connectedServices/requestAuth/**` | `openai-codex` and Claude subscription OAuth use qualified per-attempt request auth; OpenAI/Anthropic API keys and Claude setup tokens remain native. OAuth tokens never enter OpenCode auth content, and the retired launch-member broker path is removed. | Add live OAuth/quota coverage for the composed daemon → OpenCode interceptor path. |
| Unit/integration test surface | `supported` | `packages/plugins/opencode/src/agent/**` | Coverage is strong for runtime, attach, direct-session, handoff, and transport parsing; most server-native tests are CLI-local rather than full UI flows. | Preserve current provider-local tests, but reorganize around unified provider capability contracts so server/ACP/direct tests share common fixtures. |
| Provider baseline / contract tests | `supported` | `packages/plugins/opencode/src/agent/**`, `packages/tests/src/testkit/providers/scenarios/scenarios.opencode.ts`, `packages/tests/suites/agents/opencode.server.sharedSession.realProbe.test.ts` | Provider baseline coverage is still ACP-heavy; server-native real probe exists separately. | Add server-native scenario coverage to the same provider contract harness so OpenCode does not look like two unrelated providers. |
| UI e2e and manual QA surfaces | `partial` | `packages/tests/suites/ui-e2e/session.direct.opencode.browse.openTail.feat.sessions.direct.spec.ts`, `apps/docs/content/docs/providers/opencode.mdx`, `apps/docs/content/docs/features/direct-sessions.mdx`, `apps/docs/content/docs/features/session-forking.mdx`, `apps/docs/content/docs/features/attach-to-session.mdx` | UI e2e currently covers direct-session browse/tail; there is no dedicated OpenCode manual-QA tracker doc comparable to some other features. | Add unified architecture QA scripts that exercise server-native spawn, attach/detach, fork, resume, and direct takeover from one checklist. |

## Cross-cutting migration themes

1. OpenCode still has two materially different runtime paths: server-native and ACP. The unified architecture should make transport an implementation detail, not a user-visible metadata split.
2. Several metadata shapes are still transport-branded even when the capability is not (`acpSessionModesV1`, `acpSessionModelsV1`, `directSessionV1`, `opencodeResumeHistoryImportV1`), although server mode now also publishes transport-neutral `sessionModesV1`.
3. Resume, direct sessions, takeover, and handoff already preserve much more of the same affinity tuple via the canonical runtime descriptor (`backendMode`, explicit server URL, remote session id, transcript source), but compatibility fallback fields still remain.
4. Execution runs are no longer ACP-only; they now default to the server-native adapter. The remaining mismatch is cleanup and parity hardening rather than missing source support.
