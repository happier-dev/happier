import { describe, it } from 'vitest';

describe.skip('core e2e: plugin lifecycle producer-backed path (TODO plugin-sdk-v1-finalization)', () => {
  it('TODO plugin-sdk-v1-finalization: installs, enables, reloads, revokes, and unloads a plugin through product entrypoints', async () => {
    throw new Error(
      'TODO plugin-sdk-v1-finalization: activate when the sibling plugin SDK v1 finalization program lands its product lifecycle contract.',
    );
  });
});
