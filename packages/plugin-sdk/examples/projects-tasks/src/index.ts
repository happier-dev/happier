// The public plugin declaration is the cold .happier-plugin/plugin.json file.
// Exporting the typed definitions keeps the direct-Data UI on that admitted API.
export { Projects, Tasks } from './collections.js';
