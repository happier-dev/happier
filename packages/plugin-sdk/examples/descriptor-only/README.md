# Descriptor-Only Plugin Example

The cold `.happier-plugin/plugin.json` manifest demonstrates a host-rendered declarative settings view,
typed settings, and scoped required/optional host access. It intentionally has no
daemon or executable UI artifact.

Install the directory, open plugin settings, and verify the declarative controls
and access-review states. This descriptor-only example needs no executable UI
artifact build.
