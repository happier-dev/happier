/**
 * Cross-layer contract version for the persisted `voiceAgentRunV1` session metadata envelope.
 *
 * Owned by protocol so UI/CLI/server tests can share a single source of truth.
 */
export const VOICE_AGENT_RUN_TRANSCRIPT_CONTRACT_VERSION = 2;
