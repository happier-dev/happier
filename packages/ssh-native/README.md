# @happier-dev/ssh-native

Optional Expo native module substrate for Happier native SSH transport.

Set `HAPPIER_ENABLE_NATIVE_SSH=1` during native/EAS builds to include the native module. When unset or `0`, app config excludes this package from Expo autolinking and callers must treat the transport as unavailable.
