{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "incremental": false,
    // The packaged-manifest assertions import the repository's authored `.mjs`
    // Windows invocation owner, so the test program reads JavaScript too.
    "allowJs": true,
    // The test tree deliberately reaches sibling workspace sources through the
    // shared testkits, so the published `src` root cannot contain the program.
    "rootDir": "../../.."
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules", "dist"]
}
