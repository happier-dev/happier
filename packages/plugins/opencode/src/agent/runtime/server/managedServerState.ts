/**
 * Stable Connected Services selection identity retained solely to keep one
 * isolated OpenCode auth selection bound to its managed-service launch.
 */
export const OPENCODE_CONNECTED_SERVICE_SELECTION_IDENTITY_ENV =
  'HAPPIER_OPENCODE_CONNECTED_SERVICE_SELECTION_IDENTITY';
