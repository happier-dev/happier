// Stage E.9 extraction lands the actual ACP backend + external sessions + connected services here.
// Currently a no-op scaffolding — see .project/plans/runtime-unification/stages/stage-E/E.9.md.
export function activate(): void {
  // intentionally empty
}
