{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "incremental": false,
    // The test tree deliberately reaches sibling workspace sources through the
    // shared testkits, so the published `src` root cannot contain the program.
    "rootDir": "../../.."
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules", "dist"]
}
