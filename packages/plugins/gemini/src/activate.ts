// Stage E.8 extraction lands the actual ACP backend + provider registration here.
// Currently a no-op scaffolding — see .project/plans/runtime-unification/stages/stage-E/E.8.md.
export function activate(): void {
  // intentionally empty
}
