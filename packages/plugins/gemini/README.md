First-party Gemini plugin package for Stage E.8.

Current status: focused plugin/projection/runtimeCore shell. The package owns Gemini's plugin manifest, activation bridge, `agentCliRuntime` definition, ACP definition shell, and generated UI projection inputs.

Broad Gemini native extraction remains held in the Stage E.8 packet. Host-local native leaves for dynamic ACP auth/meta, MCP temp-home cleanup, execution-run runtime, prompt/diff/transcript helpers, connected-service/auth/cloud/CLI hooks, protocol provider residue, and UI provider residue are still transitional source owners until parity work lands.
