// Stage E.6 extraction lands the actual ACP backend definition + protocol catalog here.
// Currently a no-op scaffolding — see .project/plans/runtime-unification/stages/stage-E/E.6.md.
// Kiro is a reservation-only placeholder until the canonical vendor-incomplete
// contract exists in protocol + runtime surfaces.
export function activate(): void {
  // intentionally empty
}
