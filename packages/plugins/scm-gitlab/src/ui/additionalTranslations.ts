export const GITLAB_ADDITIONAL_UI_TRANSLATIONS = Object.freeze({
  "en": Object.freeze({
    "plugins.gitlab.ui.rereadApprovals": "Re-read the approvals from GitLab",
  }),
  "ru": Object.freeze({
    "plugins.gitlab.ui.rereadApprovals": "Повторно прочитать одобрения из GitLab",
  }),
  "pl": Object.freeze({
    "plugins.gitlab.ui.rereadApprovals": "Ponownie wczytaj zatwierdzenia z GitLab",
  }),
  "es": Object.freeze({
    "plugins.gitlab.ui.rereadApprovals": "Vuelve a leer las aprobaciones de GitLab",
  }),
  "fr": Object.freeze({
    "plugins.gitlab.ui.rereadApprovals": "Renvoie les approuvements depuis GitLab",
  }),
  "it": Object.freeze({
    "plugins.gitlab.ui.rereadApprovals": "Ricarica gli approvazioni da GitLab",
  }),
  "pt": Object.freeze({
    "plugins.gitlab.ui.rereadApprovals": "Volte a ler as aprovações no GitLab",
  }),
  "ca": Object.freeze({
    "plugins.gitlab.ui.rereadApprovals": "Rerebuda les aprovacions des de GitLab",
  }),
  "zh-Hans": Object.freeze({
    "plugins.gitlab.ui.rereadApprovals": "从 GitLab 重新读取批准",
  }),
  "zh-Hant": Object.freeze({
    "plugins.gitlab.ui.rereadApprovals": "從 GitLab 重新讀取批准",
  }),
  "ja": Object.freeze({
    "plugins.gitlab.ui.rereadApprovals": "GitLab の承認を再読み取る",
  }),
});

