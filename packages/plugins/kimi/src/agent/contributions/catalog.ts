// The generated bundled-plugin binding still imports this contribution. Kimi
// has no remaining host-private runtime overlay: its Session preferences are
// declared through the public `cliSessionCommand` registration in manifest.ts.
export const KIMI_AGENT_RUNTIME_CONTRIBUTION = Object.freeze({});
