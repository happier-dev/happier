// Stage E.4 extraction lands the actual ACP backend + activate-hook ops here.
// Currently a no-op scaffolding — see .project/plans/runtime-unification/stages/stage-E/E.4.md.
export function activate(): void {
  // intentionally empty
}
