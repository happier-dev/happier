Skeleton plugin package for `kilo` reserved per amendment-x Wave C 2026-04-27. Substantive content lands during Stage E.5 extraction (see `.project/plans/runtime-unification/stages/stage-E/E.5.md`). Currently no-op scaffolding.

Note: source has L-2 layout violations (`src/agent/acp.ts` plus a flat `src/permissions/**`); the canonical extracted layout will live under `src/agent/{acp,permissions}/` per L-2.
