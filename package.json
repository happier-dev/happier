{
    "name": "monorepo",
    "private": true,
    "scripts": {
        "build:packages": "yarn workspace @happier-dev/agents build && yarn workspace @happier-dev/protocol build",
        "ci:act": "bash scripts/ci/run-act-tests.sh",
        "test": "yarn --cwd apps/ui test && yarn --cwd apps/cli test && yarn --cwd apps/server test",
        "typecheck": "yarn --cwd apps/ui typecheck && yarn --cwd apps/cli typecheck && yarn --cwd apps/server typecheck"
    },
    "workspaces": {
        "packages": [
            "apps/ui",
            "apps/cli",
            "apps/server",
            "apps/stack",
            "packages/agents",
            "packages/protocol"
        ],
        "nohoist": [
            "**/zod",
            "**/react",
            "**/react-dom",
            "**/react-native",
            "**/react-native/**",
            "**/react-native-edge-to-edge/**",
            "**/react-native-incall-manager/**"
        ]
    },
    "packageManager": "yarn@1.22.22"
}
